\chapter{Power Series and Fourier Analysis}\label{ch:infinite-series}

The second route to the same circle quantity is the arctangent power series.
It comes after the effective FTC: finite coefficient identities and tail
bounds can now be interpreted as integrals and primitives when needed.
An infinite object is represented by a finite prefix and a certified tail.
\begin{verbatim}
prefix -> finite identity -> tail bound -> nested enclosure
\end{verbatim}

\section{Finite Fourier analysis}

Fourier analysis enters first as an exact finite transform on rational-complex
lists. Root-of-unity and cancellation facts are explicit certificates; no
infinite function space is assumed.
\begin{verbatim}
complex samples -> finite transform -> orthogonality/cancellation -> coefficients
\end{verbatim}
The four-point transform, convolution, Parseval-style norm identities, and
block repetition are checked finite statements.  The next layer is now also
formalized: a stagewise coordinate-tail certificate turns finite transforms
into a shrinking raw complex algorithm.  The geometric quarter-turn family
provides a nonzero instance with radius \(2r^n\) for \(0\le r\le 1/2\).

The finite Fourier API is explicit:

\lean{ComputableAnalysis.finiteFourierSum}
\lean{ComputableAnalysis.fourPointFourierTransform_reconstruct}
\lean{ComputableAnalysis.fourPointFourierTransform_parseval}
\lean{ComputableAnalysis.FiniteFourierOrthogonalityCertificate}
\lean{ComputableAnalysis.FiniteFourierReconstructionCertificate.reconstructs}
\leanok

The infinite-looking step is only a tail certificate over these finite
transforms:

\begin{verbatim}
finite transform -> coordinate tail box -> future-stage containment
                 -> stabilized complex raw -> requested-width witness
\end{verbatim}

\lean{ComputableAnalysis.EffectiveFourierTailCertificate}
\lean{ComputableAnalysis.EffectiveFourierTailCertificate.toSeries_valid}
\lean{ComputableAnalysis.EffectiveFourierSeries.stabilized_valid}
\lean{ComputableAnalysis.EffectiveFourierSeries.precision_witness}
\lean{ComputableAnalysis.quarterTurnGeometricTailCertificate_valid}
\leanok

The first genuinely nonzero infinite-looking example is the quarter-turn
geometric Fourier family.  Its public object is still assembled from finite
coefficient stages: the radius \(2r^n\) controls every future stage, and the
factorial or trigonometric interpretation is not needed to define the raw
complex value.

\lean{ComputableAnalysis.quarterTurnGeometricFourierSeries}
\lean{ComputableAnalysis.quarterTurnGeometricFourierSeries_future_stage_enclosure}
\leanok

\section{Series as computation}
\label{thm:arithmetic-series-sum}
\label{thm:finite-geometric-series-interface}
\label{thm:harmonic-dyadic-lower-bound}
\label{thm:finite-fourier-interface}
\label{thm:finite-prime-reciprocal-interface}
\label{thm:finite-reciprocal-triangular-interface}
\label{thm:finite-power-sum-interface}
\label{thm:finite-stirling-certificate}

Geometric, arithmetic, alternating, power-sum, Fourier, and reciprocal-series
examples use one interface. Divergence is expressed as potential infinity:
every finite target is eventually reached, with an explicit stage.

The convergence claim is never a completed infinite sum.  It is a pair of
finite obligations: later prefixes stay inside the earlier enclosure, and a
chosen tail budget eventually fits inside any requested rational width.
\begin{verbatim}
N := choose_tail_budget(eps)
prefix := sum(term, 0, N)
return prefix +/- certified_tail(N)
\end{verbatim}

The reusable series contract is:

\begin{verbatim}
term evaluator + finite algebra + tail bound
                  -> valid shrinking realRaw
                  -> equivalence with another computation
\end{verbatim}

Termwise differentiation or integration is admitted only after the finite
coefficient identity and the transported tail bound have both been proved.
This is the series analogue of the effective FTC: a global identity is a
chain of finite interval containments.

Concrete scalar series instantiate the same contract. The geometric raw and
alternating raw examples make the two obligations visible: stage nesting is
proved from finite partial-sum algebra, while eventual precision is proved
from a rational tail bound.

The geometric example packages this tail budget explicitly: a finite prefix,
an endpoint formula, and a power inequality certify the remaining gap.

\lean{ComputableAnalysis.Series.FiniteGeometricSeriesCertificate}
\lean{ComputableAnalysis.Series.FiniteGeometricSeriesCertificate.gap_le_tolerance}
\lean{ComputableAnalysis.geometricRaw_reaches_of_positive_tolerance}
\leanok

\lean{ComputableAnalysis.geometricRaw_valid_of_le_half}
\lean{ComputableAnalysis.geometricRaw_equiv_inv_one_sub}
\lean{ComputableAnalysis.AlternatingRaw.toRealRaw_valid}
\lean{ComputableAnalysis.leibnizAlternatingRaw_valid}
\leanok

\section{Pi and power series}
\label{thm:shared-rational-anchor}
\label{thm:leibniz-arctan}

The project studies the space of computations, not only the fastest one.
Crude rectangles, binomial expansions, termwise integration, and alternating
series can be compared through a shared rational anchor.
\begin{verbatim}
choose route -> compute prefix -> compare enclosures -> refine
\end{verbatim}
This is the setting for Leibniz's series and the faster power-series routes to
$\pi$: the historical lesson is methodological, not a claim about Newton's
private reasoning.

Termwise differentiation and integration are finite coefficient identities
followed by a tail transport certificate.  The effective FTC in
Chapter~\ref{ch:integrals} supplies the corresponding endpoint interpretation
when a series is used as a primitive.

The finite integration identity is already closed before any tail argument:
extending an integrated Taylor prefix adds one explicit endpoint monomial, and
folding those increments gives the complete finite prefix formula.

\lean{ComputableAnalysis.FinitePolynomial.finiteTaylorFTC_step}
\lean{ComputableAnalysis.FinitePolynomial.finiteTaylorFTC_prefix}
\leanok

The formal coefficient layer already records the derivative shifts for the
main complex-valued functions:

\lean{ComputableAnalysis.PowerSeries.atanTaylorCoeff_hasFormalDerivative}
\lean{ComputableAnalysis.PowerSeries.expCoeff_hasFormalDerivative}
\lean{ComputableAnalysis.PowerSeries.sinCoeff_hasFormalDerivative}
\lean{ComputableAnalysis.PowerSeries.cosCoeff_hasFormalDerivative}
\leanok

The same separation is used when a finite-prefix or partition schedule is not
nested on its own: a valid anchor and a rational width budget are used only in
the proof to prefix-stabilize the runtime boxes.  The stabilized computation
is then the public raw representative; no limit object is introduced behind
the notation.

\lean{ComputableAnalysis.RealRaw.prefixStabilize_valid}
\lean{ComputableAnalysis.RealRaw.prefixStabilize_equiv_anchor}
\leanok

These are formal power-series identities.  An analytic theorem additionally
supplies a finite tail certificate on the chosen rational domain.

For the arctangent route, the alternating tail is explicit and the rational
anchor is shared with the rectangle computation.  Faster Machin/Newton-style
series are then compared by overlap rather than declared to be the definition
of the same constant.

\section{Basel and factorial growth}
\label{thm:basel-geometric-overlap}

Basel compares two certified computations of the same quantity: reciprocal
square prefixes and geometric area. Factorial growth supplies the tail bounds
used by Stirling-type and Gaussian estimates.
\begin{verbatim}
algorithm A -> interval A       algorithm B -> interval B
                         \        /
                          overlap
\end{verbatim}
