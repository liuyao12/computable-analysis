\chapter{Infinite Series}\label{ch:infinite-series}

An infinite object is a sequence of finite computations with a certified tail.

\section{Finite stages}

Fourier, Parseval, power sums, geometric sums, and prime-reciprocal prefixes
are finite certificates, not completed limits.
\begin{verbatim}
forward := finite mode sums
inverse := recombine coordinates / sample count
tail   := explicit rational bound
\end{verbatim}
\label{sec:series-benchmark-foundations}\label{thm:finite-fourier-orthogonality}
\label{thm:finite-fourier-interface}
\emph{Fourier interface.} A finite rational transform reconstructs its sample
vector and satisfies a finite Parseval identity; no infinite series is hidden.
\label{thm:finite-prime-reciprocal-extension}\label{thm:arithmetic-series-sum}
\emph{Arithmetic interface.} A finite progression is folded exactly and checked against its closed form.
\begin{verbatim}
prefix := arithmeticProgressionSum initial difference stage
closed := stage * (2*initial + (stage-1)*difference) / 2
\end{verbatim}
\lean{ComputableAnalysis.Series.FiniteArithmeticSeriesCertificate}
\lean{ComputableAnalysis.Series.FiniteArithmeticSeriesCertificate.closed_form}
\lean{ComputableAnalysis.Series.FiniteArithmeticSeriesCertificate.succ_step}
\lean{ComputableAnalysis.Series.finiteArithmeticSeriesCertificate}
\label{thm:finite-power-sums}\label{thm:geometric-series-sum}
\emph{Binomial interface.} A finite row is evaluated until all coefficients are present; the exact fold equals the corresponding power.
\begin{verbatim}
row := binomialSum exponent left right stage
require exponent + 1 <= stage
assert row = (left + right)^exponent
\end{verbatim}
\lean{ComputableAnalysis.Series.FiniteBinomialSeriesCertificate}
\lean{ComputableAnalysis.Series.FiniteBinomialSeriesCertificate.power_identity}
\lean{ComputableAnalysis.Series.finiteBinomialSeriesCertificate}
\label{thm:finite-geometric-series-interface}
\emph{Geometric interface.} Compute the finite prefix recursively and carry
an explicit rational tail bound.  The certificate exposes only a finite stage;
the completed sum is a later identification.
\begin{verbatim}
prefix := geometricSum ratio stage
endpoint := 1 / (1 - ratio)
require ratio^stage <= tolerance * (1 - ratio)
\end{verbatim}
\lean{ComputableAnalysis.Series.FiniteGeometricSeriesCertificate}
\lean{ComputableAnalysis.Series.FiniteGeometricSeriesCertificate.prefix_le_endpoint}
\lean{ComputableAnalysis.Series.FiniteGeometricSeriesCertificate.gap_le_tolerance}
\lean{ComputableAnalysis.Series.finiteGeometricSeriesCertificate}
\label{thm:reciprocal-triangular-sum}\label{thm:harmonic-dyadic-lower-bound}
\emph{Harmonic growth interface.} For each natural target, compute an explicit dyadic stage whose finite harmonic prefix reaches it.
\begin{verbatim}
stage := 2^(2*target)
assert target <= harmonicSum stage
\end{verbatim}
\lean{ComputableAnalysis.FiniteHarmonicGrowthCertificate}
\lean{ComputableAnalysis.FiniteHarmonicGrowthCertificate.explicit_stage}
\lean{ComputableAnalysis.finiteHarmonicGrowthCertificate}
\label{thm:finite-reciprocal-triangular-interface}
\lean{ComputableAnalysis.Series.ReciprocalTriangularCertificate}
\lean{ComputableAnalysis.Series.ReciprocalTriangularCertificate.exact_prefix}
\lean{ComputableAnalysis.Series.ReciprocalTriangularCertificate.tail_formula}
\label{thm:finite-power-sum-interface}
\lean{ComputableAnalysis.Series.FinitePowerSumCertificate}
\lean{ComputableAnalysis.Series.FinitePowerSumCertificate.succ_step}
\lean{ComputableAnalysis.Series.FinitePowerSumCertificate.growth_bound}
\emph{Finite sum interfaces.} Reciprocal triangular terms telescope with an
explicit tail, while power sums have checked polynomial forms at finite stage.
\label{thm:finite-prime-reciprocal-interface}
The prime-reciprocal construction extends any finite certified prime list and
strictly raises its rational accumulator; divergence is the potential-infinity
interpretation of this extension.

\section{Pi and power series}

Archimedean rectangles, binomial series, termwise integration, and Machin
identities are different routes through one space of computations.  This is
a methodological remark, not a claim about Newton's motives.
\label{def:alternating-series-intervals}\label{thm:nilakantha-pi}
\label{thm:power-series-arctan-area-pi}\label{thm:leibniz-arctan}
\emph{Alternating interface.} Keep the finite interval between consecutive partial sums; its width is the current term magnitude.
\begin{verbatim}
interval := between(partialSum(2*stage), partialSum(2*stage+1))
width := term(2*stage)
\end{verbatim}
\lean{ComputableAnalysis.Series.FiniteAlternatingSeriesCertificate}
\lean{ComputableAnalysis.Series.FiniteAlternatingSeriesCertificate.width_eq}
\lean{ComputableAnalysis.Series.finiteAlternatingSeriesCertificate}
\label{def:formal-power-series}\label{def:effective-derivative}
\label{def:power-series-geometric-majorant}\label{def:complex-power-series-evaluators}

\section{Basel and factorial growth}

Finite ratio and comparison certificates precede constructive moduli for the
full limits.
\label{thm:finite-stirling-certificate}\label{thm:finite-factorial-prefix}
\lean{ComputableAnalysis.FiniteFactorialCertificate}
\lean{ComputableAnalysis.FiniteFactorialCertificate.succ_step}
\lean{ComputableAnalysis.FiniteFactorialCertificate.positive}
\label{thm:finite-stirling-interface}
\emph{Stirling boundary.} The current certificate bounds selected finite
ratios; it does not assert the completed asymptotic formula.
\label{thm:basel-comparison-example}\label{thm:basel-supplied-target-containment}
\label{thm:basel-geometric-rhs-valid}\label{conj:basel}

The equality interface is an eventual shared rational anchor: two nested
interval programs that keep containing the same rational witness represent the
same computable real.  The Basel identity still needs a witness that persists
at every later stage.
\label{thm:shared-rational-anchor}
