\hypersetup{
  colorlinks=true,
  linkcolor=blue,
  citecolor=blue,
  urlcolor=blue
}

% Cross-chapter theorem links in the preface need explicit HTML targets.
\newcommand{\benchmarklink}[3]{\href{#1.html\##2}{#3}}

% Web macros use the looping GIF; the print macros deliberately select each
% corresponding final PNG frame instead.
\newcommand{\rationalCircleSubdivisionAnimation}{%
  \includegraphics[width=360pt]{assets/rational-circle-subdivision.gif}%
}

\newcommand{\arctanRectangleEnclosureAnimation}{%
  \includegraphics[width=360pt]{assets/arctan-rectangle-enclosure.gif}%
}

\newcommand{\intervalSineIntegralStageAnimation}{%
  \includegraphics[width=360pt]{assets/interval-sine-integral-stage.gif}%
}

\newcommand{\sqrtSecantTangentAnimation}{%
  \includegraphics[width=250pt]{assets/sqrt-secant-tangent.gif}%
}

\newcommand{\integrationByPartsAnimation}{%
  \includegraphics[width=360pt]{assets/integration-by-parts-cell.gif}%
}

\newcommand{\substitutionPartitionAnimation}{%
  \includegraphics[width=360pt]{assets/substitution-partition.gif}%
}

\newcommand{\singleTurnIntegralAnimation}{%
  \includegraphics[width=360pt]{assets/single-turn-integral.gif}%
}

\newcommand{\ftcEndpointAnimation}{%
  \includegraphics[width=360pt]{assets/ftc-endpoint-comparison.gif}%
}

\newcommand{\peanoBakerWordsAnimation}{%
  \includegraphics[width=320pt]{assets/peano-baker-words.gif}%
}
