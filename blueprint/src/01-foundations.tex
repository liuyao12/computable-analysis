\chapter{Computable Numbers and Functions}\label{ch:foundations}\n\n\begin{theorem}[Rational numerator--denominator coding]\n\label{thm:rational-code-surjection}\n\lean{ComputableAnalysis.RationalCode}\n\lean{ComputableAnalysis.RationalCode.encode}\n\lean{ComputableAnalysis.RationalCode.decode}\n\lean{ComputableAnalysis.RationalCode.den_pos}\n\lean{ComputableAnalysis.RationalCode.decode_encode}\n\lean{ComputableAnalysis.RationalCode.encode_injective}\n\lean{ComputableAnalysis.RationalCode.encode_eq_iff}\n\lean{ComputableAnalysis.rationalCode_decode_surjective}\n\lean{ComputableAnalysis.triangular}\n\lean{ComputableAnalysis.triangular_succ}\n\lean{ComputableAnalysis.triangular_monotone}\n\lean{ComputableAnalysis.diagonalPair}\n\lean{ComputableAnalysis.diagonalUnpair}\n\lean{ComputableAnalysis.diagonalUnpair_spec}\n\lean{ComputableAnalysis.diagonalUnpair_diagonalPair}\n\lean{ComputableAnalysis.diagonalPair_injective}\n\lean{ComputableAnalysis.diagonalPair_surjective}\n\lean{ComputableAnalysis.integerCode}\n\lean{ComputableAnalysis.integerCode_injective}\n\lean{ComputableAnalysis.integerCode_surjective}\n\lean{ComputableAnalysis.rationalNatCode}\n\lean{ComputableAnalysis.rationalNatCode_injective}\n\lean{ComputableAnalysis.rationalNatCode_encode_surjective}\n\lean{ComputableAnalysis.rationalNatCode_existsUnique_canonical_index}\n\lean{ComputableAnalysis.rationalNatCode_existsUnique_canonical_decode_index}\n\lean{ComputableAnalysis.rationalNatCode_decode_surjective}\n\lean{ComputableAnalysis.rationalNatCode_index}\n\lean{ComputableAnalysis.rationalNatIndex_three_fifths}\n\lean{ComputableAnalysis.rationalNatCode_index_three_fifths}\n\lean{ComputableAnalysis.rationalNatCode_decode_three_fifths}\n\lean{ComputableAnalysis.rationalNatIndex}\n\lean{ComputableAnalysis.rationalNatIndex_injective}\n\lean{ComputableAnalysis.rationalNatCode_index}\n\leanok\nEvery rational has a finite code consisting of an integer numerator and a\npositive natural denominator, and the normalization decoder is surjective\nonto the project's rational type.  The diagonal pairing, signed-integer code,\nand positive-denominator shift combine into the explicit natural-number\ndecoder \texttt{rationalNatCode}, whose surjectivity is checked by\n`rationalNatCode_decode_surjective`; `diagonalPair_injective` and\n`rationalNatCode_injective` also certify that the natural-number code path is\ninjective before decoding.  The stronger\n`rationalNatCode_encode_surjective` statement shows that every canonical\nnumerator--denominator code is hit exactly before decoding.  This is the\n`rationalNatCode_existsUnique_canonical_index` refinement of that statement:\nthe canonical code has a unique natural-number index.  This is the\nconstructive coding core of benchmark item 3 (denumerability of the rationals).\nThe canonical decode-index theorem makes the normalization choice unique at\nthe finite code level.  The explicit `rationalNatIndex` and\n`rationalNatIndex_injective` now provide the matching injection from canonical\nrationals to natural indices, while retaining the finite-code interface.  The\nsurjection and injection together complete the project's coding-level\ndenumerability boundary for benchmark item 3.\nAs a concrete audit point, the canonical index of (3/5) is (61), and Lean\nchecks that decoding the enumerated code at (61) returns (3/5).\n\end{theorem}\n\n\begin{theorem}[Finite induction schema]\n\label{thm:nat-induction-schema}\n\lean{ComputableAnalysis.nat_induction_schema}\n\leanok\nFor any proposition-valued predicate on the natural numbers, a proof at zero\nand a proof step from $n$ to $n+1$ produce a proof at every natural number.\nThis is the checked proof/programming principle used by the project's finite\ncertificate constructions, and is the project's scoped formalization of\nbenchmark item 74.\n\end{theorem}\n\n\begin{theorem}[Finite monotone-sequence propagation]\n\label{thm:finite-monotone-sequence}
\emph{Monotone interface.} A successor inequality propagates to every finite pair of stages.
\begin{verbatim}
require sequence[n] <= sequence[n+1] for every n
assert sequence[a] <= sequence[b] whenever a <= b
\end{verbatim}
\lean{ComputableAnalysis.FiniteAscendingSequenceCertificate}
\lean{ComputableAnalysis.FiniteAscendingSequenceCertificate.pair_le}
\lean{ComputableAnalysis.FiniteDescendingSequenceCertificate}
\lean{ComputableAnalysis.FiniteDescendingSequenceCertificate.pair_le}
\lean{ComputableAnalysis.finiteAscendingSequenceCertificate}
\lean{ComputableAnalysis.finiteDescendingSequenceCertificate}\n\lean{ComputableAnalysis.monotone_of_succ_le}\n\lean{ComputableAnalysis.antitone_of_succ_ge}\n\lean{ComputableAnalysis.ascendingNaturalSequence}\n\lean{ComputableAnalysis.ascendingNaturalSequence_monotone}\n\lean{ComputableAnalysis.ascendingNaturalSequence_stage64}\n\lean{ComputableAnalysis.ascendingNaturalSequence_stage128}\n\lean{ComputableAnalysis.dyadicApproach}\n\lean{ComputableAnalysis.dyadicApproach_succ_le}\n\lean{ComputableAnalysis.dyadicApproach_le_one}\n\lean{ComputableAnalysis.dyadicApproach_error}\n\lean{ComputableAnalysis.dyadicApproach_error_shrinks}\n\lean{ComputableAnalysis.dyadicApproach_stage8_certificate}\n\lean{ComputableAnalysis.dyadicApproach_stage16_certificate}\n\lean{ComputableAnalysis.dyadicApproach_stage32_certificate}\n\lean{ComputableAnalysis.dyadicApproach_stage64_certificate}\n\lean{ComputableAnalysis.dyadicApproach_stage128_certificate}\n\leanok\nIf each successor step of a rational sequence is nondecreasing, the same\norder is propagated to every finite pair of indices; the dual successor\ncondition gives an antitone sequence.  The executable sequence\n\texttt{ascendingNaturalSequence} supplies a concrete ascending witness at\nstages 64 and 128.  The bounded dyadic sequence\n\(1-2^{-n}\) is also checked stage by stage: it is nondecreasing, bounded by\n1, and has an explicit rational error certificate (at stage 8, the error is\n\(1/256\)).  This is the project's potential-infinity core for benchmark item\n73.  The error-shrinking theorem supplies a denominator-derived stage for\nevery positive rational tolerance, while no attained real limit is asserted.\n\end{theorem}\n\n\begin{theorem}[Finite subset and binomial recurrences]\n\label{thm:finite-counting-recurrences}\n\lean{ComputableAnalysis.FiniteCounting.subsetCount}\n\lean{ComputableAnalysis.FiniteCounting.subsetCount_succ}\n\lean{ComputableAnalysis.FiniteCounting.subsetCount_eq_pow}\n\lean{ComputableAnalysis.FiniteCounting.combination}\n\lean{ComputableAnalysis.FiniteCounting.combination_zero_right}\n\lean{ComputableAnalysis.FiniteCounting.combination_zero_left}\n\lean{ComputableAnalysis.FiniteCounting.combination_pascal}\n\lean{ComputableAnalysis.FiniteCounting.combination_rat_pascal}\n\lean{ComputableAnalysis.FiniteCounting.combination_outside}\n\lean{ComputableAnalysis.FiniteCounting.combination_rat_outside}\n\lean{ComputableAnalysis.FiniteCounting.combination_self}\n\lean{ComputableAnalysis.FiniteCounting.combination_rat_self}\n\lean{ComputableAnalysis.FiniteCounting.combination_one}\n\lean{ComputableAnalysis.FiniteCounting.combination_two_rat}\n\lean{ComputableAnalysis.FiniteCounting.combination_three_rat}\n\lean{ComputableAnalysis.FiniteCounting.combination_four_rat}\n\lean{ComputableAnalysis.FiniteCounting.combination_five_rat}\n\lean{ComputableAnalysis.FiniteCounting.combination_six_rat}\n\lean{ComputableAnalysis.FiniteCounting.combination_seven_rat}\n\lean{ComputableAnalysis.FiniteCounting.combination_eight_rat}\n\lean{ComputableAnalysis.pascal_combination_8_3}\n\lean{ComputableAnalysis.pascal_combination_7_2}\n\lean{ComputableAnalysis.pascal_combination_7_3}\n\lean{ComputableAnalysis.pascal_combination_8_3_recurrence}\n\lean{ComputableAnalysis.pascal_combination_8_3_certificate}\n\lean{ComputableAnalysis.pascal_combination_10_3}\n\lean{ComputableAnalysis.pascal_combination_9_2}\n\lean{ComputableAnalysis.pascal_combination_9_3}\n\lean{ComputableAnalysis.pascal_combination_10_3_recurrence}\n\lean{ComputableAnalysis.pascal_combination_10_3_certificate}\n\lean{ComputableAnalysis.pascal_combination_15_3}\n\lean{ComputableAnalysis.pascal_combination_14_2}\n\lean{ComputableAnalysis.pascal_combination_14_3}\n\lean{ComputableAnalysis.pascal_combination_15_3_recurrence}\n\lean{ComputableAnalysis.pascal_combination_15_3_certificate}\n\lean{ComputableAnalysis.pascal_combination_20_3}\n\lean{ComputableAnalysis.pascal_combination_19_2}\n\lean{ComputableAnalysis.pascal_combination_19_3}\n\lean{ComputableAnalysis.pascal_combination_20_3_recurrence}\n\lean{ComputableAnalysis.pascal_combination_20_3_certificate}\n\lean{ComputableAnalysis.pascal_combination_25_3}\n\lean{ComputableAnalysis.pascal_combination_24_2}\n\lean{ComputableAnalysis.pascal_combination_24_3}\n\lean{ComputableAnalysis.pascal_combination_25_3_recurrence}\n\lean{ComputableAnalysis.pascal_combination_25_3_certificate}\n\lean{ComputableAnalysis.pascal_combination_32_3}\n\lean{ComputableAnalysis.pascal_combination_31_2}\n\lean{ComputableAnalysis.pascal_combination_31_3}\n\lean{ComputableAnalysis.pascal_combination_32_3_recurrence}\n\lean{ComputableAnalysis.pascal_combination_32_3_certificate}\n\leanok\nThe finite recurrence \texttt{subsetCount(n+1)=2*subsetCount(n)} gives the\ncertificate core of the number-of-subsets theorem (benchmark item 52), while\n\texttt{combination} supplies Pascal's recurrence, its rational bridge\n`combination\_rat_pascal`, and its zero-outside-range boundary for\nbinomial coefficients (benchmark item 58), together with rationally cast\noutside-range and diagonal boundaries and the first-row identity\n`combination_one`.  These are finite\ncounting interfaces; a general finite-set cardinality library is intentionally\nnot introduced here.\nThe worked subset counter now also reaches stage 16, with exact value\n\(2^{16}=65536\), giving a finer finite checkpoint for item 52.\nIt now also reaches stage 64, with exact value\n\(2^{64}=18446744073709551616\), extending the terminating recurrence\ncertificate.\nThe Pascal witness now reaches \(\binom{20}{3}=1140\), with the checked\ndecomposition \(1140=171+969\) from the two predecessor coefficients.\nIt now also reaches \(\binom{32}{3}=4960\), with the checked decomposition\n\(4960=465+4495\), extending the finite recurrence schedule.\nIt now also reaches \(\binom{25}{3}=2300\), with the decomposition\n\(2300=276+2024\) checked from its two predecessor coefficients.\n\end{theorem}\n\n\begin{theorem}[Certificate-level fundamental theorem of arithmetic]\n\label{thm:finite-fta-factorization}\n\label{rem:finite-factorization-certificates}\n\lean{ComputableAnalysis.MultiplicativeCertificate}\n\lean{ComputableAnalysis.factorizationCertificate60}\n\lean{ComputableAnalysis.primeFactorizationCertificate360}\n\lean{ComputableAnalysis.primeFactorizationCertificate360_factors}\n\lean{ComputableAnalysis.primeFactorizationCertificate360_product}\n\lean{ComputableAnalysis.primeFactorizationCertificate360_factor_dvd}\n\lean{ComputableAnalysis.primeFactorizationCertificate360_alternative}\n\lean{ComputableAnalysis.primeFactorizationCertificate360_alternative_factors}\n\lean{ComputableAnalysis.primeFactorizationCertificate360_factor_order_unique}\n\lean{ComputableAnalysis.primeFactorizationCertificate720}\n\lean{ComputableAnalysis.primeFactorizationCertificate720_product}\n\lean{ComputableAnalysis.primeFactorizationCertificate720_factor_dvd}\n\lean{ComputableAnalysis.primeFactorizationCertificate600}\n\lean{ComputableAnalysis.primeFactorizationCertificate600_product}\n\lean{ComputableAnalysis.primeFactorizationCertificate600_factor_dvd}\n\lean{ComputableAnalysis.primeFactorizationCertificate126}\n\lean{ComputableAnalysis.primeFactorizationCertificate126_product}\n\lean{ComputableAnalysis.primeFactorizationCertificate126_factor_dvd}\n\lean{ComputableAnalysis.primeFactorizationCertificate27720}\n\lean{ComputableAnalysis.primeFactorizationCertificate27720_product}\n\lean{ComputableAnalysis.primeFactorizationCertificate27720_factor_dvd}\n\lean{ComputableAnalysis.BasicPrime}\n\lean{ComputableAnalysis.basicPrime_of_no_proper_divisor}\n\lean{ComputableAnalysis.basicPrime_iff_no_proper_divisor}\n\lean{ComputableAnalysis.properDivisorSearch}\n\lean{ComputableAnalysis.properDivisorSearch_some_is_proper}\n\lean{ComputableAnalysis.properDivisorSearch_some_of_proper}\n\lean{ComputableAnalysis.properDivisorSearch_none_of_no_proper}\n\lean{ComputableAnalysis.properDivisorSearch_none_iff_no_proper}\n\lean{ComputableAnalysis.basicPrime_of_properDivisorSearch_none}\n\lean{ComputableAnalysis.basicPrime_two}\n\lean{ComputableAnalysis.basicPrime_three}\n\lean{ComputableAnalysis.basicPrime_five}\n\lean{ComputableAnalysis.basicPrime_eq_of_dvd}\n\lean{ComputableAnalysis.basicPrime_coprime_of_not_dvd}\n\lean{ComputableAnalysis.basicPrime_dvd_of_dvd_mul}\n\lean{ComputableAnalysis.basicPrime_dvd_of_dvd_pow}\n\lean{ComputableAnalysis.natProduct}\n\lean{ComputableAnalysis.natProduct_perm}\n\lean{ComputableAnalysis.list_mem_dvd_natProduct}\n\lean{ComputableAnalysis.foldl_mul_one_eq_natProduct}\n\lean{ComputableAnalysis.foldl_mul_acc_eq_acc_mul_foldl_one}\n\lean{ComputableAnalysis.MultiplicativeCertificate.append}\n\lean{ComputableAnalysis.MultiplicativeCertificate.factors_nonempty}\n\lean{ComputableAnalysis.MultiplicativeCertificate.factor_dvd}\n\lean{ComputableAnalysis.basicPrime_dvd_of_dvd_natProduct}\n\lean{ComputableAnalysis.PrimeFactorCertificate}\n\lean{ComputableAnalysis.PrimeFactorCertificate.append}\n\lean{ComputableAnalysis.primeFactorCertificate_exists}\n\lean{ComputableAnalysis.exists_basicPrime_dvd}\n\lean{ComputableAnalysis.exists_basicPrime_not_mem_of_all_basicPrime}\n\lean{ComputableAnalysis.shiftedRangeProduct}\n\lean{ComputableAnalysis.exists_basicPrime_gt}\n\lean{ComputableAnalysis.PrimeFactorCertificate.factor_dvd}\n\lean{ComputableAnalysis.PrimeFactorCertificate.factor_le}\n\lean{ComputableAnalysis.PrimeFactorCertificate.factor_le}\n\lean{ComputableAnalysis.PrimeFactorCertificate.factors_nonempty}\n\lean{ComputableAnalysis.PrimeFactorCertificate.exists_prime_dvd}\n\lean{ComputableAnalysis.PrimeFactorCertificate.exists_factor_dvd}\n\lean{ComputableAnalysis.basicPrime_dvd_of_dvd_primeFactorization}\n\lean{ComputableAnalysis.basicPrime_eq_factor_of_dvd_primeFactorization}\n\lean{ComputableAnalysis.basicPrime_dvd_primeFactorization_iff}\n\lean{ComputableAnalysis.PrimeFactorCertificate.factor_mem_of_factor_mem}\n\lean{ComputableAnalysis.PrimeFactorCertificate.factor_mem_iff}\n\lean{ComputableAnalysis.natList_perm_cons_of_mem}\n\lean{ComputableAnalysis.primeFactorList_perm_of_same_product}\n\lean{ComputableAnalysis.PrimeFactorCertificate.factor_perm}\n\lean{ComputableAnalysis.natList_perm_of_nodup_of_mem_iff}\n\lean{ComputableAnalysis.PrimeFactorCertificate.factor_perm_of_nodup}\n\lean{ComputableAnalysis.primeFactorizationCertificate60}\n\lean{ComputableAnalysis.primeFactorizationCertificate360}\n\lean{ComputableAnalysis.primeFactorizationCertificate360_factors}\n\lean{ComputableAnalysis.primeFactorizationCertificate360_product}\n\lean{ComputableAnalysis.primeFactorizationCertificate360_factor_dvd}\nThe foundation now has a finite certificate interface recording a list of\nnontrivial factors and its product, with the worked certificate\n\texttt{60=2\cdot2\cdot3\cdot5}.  The small primality predicate and its\ncertificates for \(2,3,5\) now make the worked certificate prime-labelled.\nThe constructor \texttt{MultiplicativeCertificate.append} composes the\nnon-prime certificate layer for products before primality is imposed.\nThe accompanying Euclid lemma, `basicPrime\_dvd\_of\_dvd_mul`, is the\nfirst reusable step toward general factorization arguments: a certified basic\nprime dividing a product divides one of its factors.\nThe finite list product \texttt{natProduct} and\n`basicPrime\_dvd\_of\_dvd_natProduct` lift this to a prime dividing\none member of a finite factor list, providing the next reusable FTA\ncertificate step.\nThe certificate-level corollary\n`basicPrime\_dvd\_of\_dvd_primeFactorization` connects this list\ntheorem directly to a \texttt{PrimeFactorCertificate}.\nThe constructor \texttt{PrimeFactorCertificate.append} composes certificates\nfor two factors while preserving both the prime labels and the product\nequation.  The companion theorem\n`PrimeFactorCertificate.factor_dvd` shows that every listed factor\ndivides the certified number, giving the converse direction to the prime\ndivisor extraction theorem.\nThe finite bound `PrimeFactorCertificate.factor_le` further places every\nlisted factor at or below the certified number whenever $1<n$, making the\ncertificate list a bounded object for finite search and validation.\nThe finite search and recursive constructor\n\n`primeFactorCertificate_exists`\n\nnow establish factorization existence for every $n>1$ at the certificate\nlevel.  The list-level theorem\n`primeFactorList\_perm\_of\_same_product`, together with\n`PrimeFactorCertificate.factor_perm`, proves uniqueness of the full\nprime-labelled factor list up to permutation, including repeated factors.\nThe Euclid construction\n`exists_basicPrime_not_mem_of_all_basicPrime` adds the finite\ninfinitude-of-primes core: from any finite list of certified primes, the\nproduct-plus-one construction returns a certified prime outside that list.\nThe specialized evaluator `shiftedRangeProduct` and theorem\n`exists_basicPrime_gt` sharpen this to an executable unboundedness statement:\nfor every finite bound (n), a certified prime (p>n) is produced.\nThis is the project's checked certificate-level core of benchmark item 80;\nthe usual classical formulation over a separately developed prime library is\noutside the current scope.\nThe worked certificate `primeFactorizationCertificate360` supplies the list\n\([2,2,2,3,3,5]), checks its product as (360), and records the resulting\ndivisibility witnesses for (2,3,5).\nThe companion certificates check the prime-labelled decompositions\n\(720=2^4\cdot3^2\cdot5\) and\n\(600=2^3\cdot3\cdot5^2\), with their products and distinct-prime\ndivisibility witnesses verified directly.\nThe additional certificate factors (126) as (2\cdot3^2\cdot7), with\nthe product and divisibility witnesses checked by terminating computation.\nThe larger certificate factors (27720) as\n(2^3\cdot3^2\cdot5\cdot7\cdot11), exercising the finite prime-search\ncertificates for both (7) and (11).\n\end{theorem}\n\n\begin{theorem}[Finite Bertrand witnesses]\n\label{thm:finite-bertrand-certificate}\n\lean{ComputableAnalysis.bertrand_stage10}\n\lean{ComputableAnalysis.bertrand_stage20}\n\lean{ComputableAnalysis.bertrand_stage30}\n\lean{ComputableAnalysis.bertrand_stage40}\n\lean{ComputableAnalysis.bertrand_stage50}\n\lean{ComputableAnalysis.bertrand_stage60}\n\lean{ComputableAnalysis.bertrand_stage70}\n\lean{ComputableAnalysis.bertrand_stage80}\n\lean{ComputableAnalysis.bertrand_stage100}\n\lean{ComputableAnalysis.bertrand_finite_certificate}\n\lean{ComputableAnalysis.bertrand_extended_finite_certificate}\n\lean{ComputableAnalysis.bertrand_further_finite_certificate}\n\leanok\nThe executable primality search verifies a prime between \(10\) and \(20\),\nnamely \(11\), and a prime between \(20\) and \(40\), namely \(23\).  These\nare finite instances of benchmark item 98.  The extended ladder also checks\n\(31\in(30,60)\), \(41\in(40,80)\), \(53\in(50,100)\),\n\(61\in(60,120)\), \(71\in(70,140)\), \(83\in(80,160)\), and\n\(101\in(100,200)\), giving nine exact finite intervals;\nthe general Bertrand postulate remains outside the current number-theory\nboundary.\n\end{theorem}\n\n\begin{theorem}[Three-digit divisibility by three]\n\label{thm:three-digit-divisibility}
\label{thm:finite-digit-divisibility-interface}
\lean{ComputableAnalysis.decimalDigitSum_divisibility_certificate}
\lean{ComputableAnalysis.decimalDigitSum_divisible_by_three_iff}\n\lean{ComputableAnalysis.three_dvd_three_digit}\n\lean{ComputableAnalysis.decimalDigitSum}\n\lean{ComputableAnalysis.decimalDigitSum_succ_of_lt_ten}\n\lean{ComputableAnalysis.decimalDigitSum_eq_self_of_lt_ten}\n\lean{ComputableAnalysis.decimalDigitSum_succ_of_not_lt_ten}\n\lean{ComputableAnalysis.decimalDigitSum_mod_three}\n\lean{ComputableAnalysis.decimalDigitSum_mod_nine_proof}\n\lean{ComputableAnalysis.decimalDigitSum_mod_nine}\n\lean{ComputableAnalysis.three_dvd_iff_decimalDigitSum_dvd}\n\lean{ComputableAnalysis.decimal_123_digit_sum}\n\lean{ComputableAnalysis.decimal_123_divisible_by_three}\n\lean{ComputableAnalysis.decimal_123_digit_sum_certificate}\n\lean{ComputableAnalysis.decimal_124_not_divisible_by_three}\n\lean{ComputableAnalysis.decimal_321_digit_sum}\n\lean{ComputableAnalysis.decimal_321_divisible_by_three}\n\lean{ComputableAnalysis.decimal_321_digit_sum_certificate}\n\lean{ComputableAnalysis.decimal_322_not_divisible_by_three}\n\lean{ComputableAnalysis.decimal_999_digit_sum}\n\lean{ComputableAnalysis.decimal_999_divisible_by_three}\n\lean{ComputableAnalysis.decimal_1000_not_divisible_by_three}\n\lean{ComputableAnalysis.decimal_123456_digit_sum}\n\lean{ComputableAnalysis.decimal_123456_divisible_by_three}\n\lean{ComputableAnalysis.decimal_123456_digit_sum_certificate}\n\lean{ComputableAnalysis.decimal_123457_not_divisible_by_three}\n\lean{ComputableAnalysis.basePositionalValue}\n\lean{ComputableAnalysis.basePositionalValue_mod_sub_one}\n\lean{ComputableAnalysis.decimalPositionalValue_mod_nine}\n\leanok\nFor natural digits (a,b,c), the decimal number (100a+10b+c) is divisible\nby (3) exactly when its digit sum (a+b+c) is divisible by (3).  This is\nthe finite arithmetic core of benchmark item 85.  The terminating decimal\ndigit-sum evaluator and its small-digit reduction are also exposed.  The\nglobal residue-preservation theorem is checked by well-founded recursion on\nthe decimal reduction.\nThe worked examples now also include the boundary pair (999) and (1000):\nthe former has digit sum (27) and is divisible by (3), while the latter is\nnot, providing a larger finite decision checkpoint.\nThe six-digit checkpoint (123456) has digit sum (21) and is divisible by (3);\nthe neighboring value (123457) supplies a terminating non-divisibility case.\n\end{theorem}\n\nThe list-based positional evaluator linked above generalizes the same finite\ndigit-sum congruence to every base (b\ge2), modulo (b-1), without assuming\na canonical digit expansion.\n\n\section{Inverval arithmetic and real numbers \humanchecked}\n\n\begin{definition}[Rational interval]\label{def:qinterval}\n\lean{ComputableAnalysis.QInterval}\nA \emph{rational interval} is a pair of rational numbers\n\[\n  [a,b]\in\Q\times\Q,\qquad a\le b.\n\]\nThis is a formal interval object, not the set of rational points between\n\(a\) and \(b\).  The \emph{width} of the rational interval is defined as \(b-a\).\n\nTwo rational intervals \([a, b]\) and \([c, d]\) \emph{overlap} when\n\[\n  a\le d\ \text{and}\ c\le b.\n\]\n\end{definition}\n\n\begin{definition}[Interval sequence]\label{def:interval-sequence}\n\uses{def:qinterval}\nAn \emph{interval sequence} is a sequence of rational intervals\n\[\n  [a_n,b_n]_{n\in\N},\n  \qquad a_n \leq b_n\in\Q.\n\]\n\end{definition}\n\n\begin{definition}[Real number]\label{def:raw-real}\n\lean{ComputableAnalysis.RealRaw}\n\uses{def:interval-sequence}\nA \emph{real number} is an interval sequence \(x=[a_n,b_n]_{n\in\N}\) satisfying the\nfollowing validity conditions:\n\[\n  n\le m\Rightarrow\n  a_n\le a_m\le b_m\le b_n,\n\]\nand for any $\eps\in\Q_{>0}$, there exists $N\in\N$ such that\n\[\n  b_N-a_N \le \eps.\n\]\n\end{definition}\n\n\begin{remark}[Computable number]\label{rem:computable-presentations}\n\uses{def:interval-sequence,def:raw-real}\nWhen the map \n\[\n  n\mapsto [a_n,b_n]\n\]\nis given by a finite, terminating algorithm, as we always do in this project,\nthe real number is said to be \emph{computable}. Storing all the rational numbers\nin an infinite table is not permitted. Evaluating at \(n\) is referred to as\n\emph{computing} the real number \emph{at stage} \(n\).\n\end{remark}\n\n\begin{definition}[Positive and negative real numbers]\label{def:raw-real-sign}\n\lean{ComputableAnalysis.RealRaw.Pos}\n\lean{ComputableAnalysis.RealRaw.Neg}\n\uses{def:raw-real}\nLet \(x=[a_n,b_n]_{n\in\N}\) be a real number.  We say that \(x\) is\n\emph{positive} (resp. \emph{negative}) if there is some \(N\in\N\) such that\n\( a_N > 0\) (resp. \( b_N<0 \)).\nThe number \(x\) is \emph{nonzero} if it is either positive or negative.\n\end{definition}\n\n\begin{definition}[Interval Arithmetic]\label{def:raw-real-arithmetic}\n{\footnotesize\raggedright\n\lean{ComputableAnalysis.RealRaw.add}\par\n\lean{ComputableAnalysis.RealRaw.sub}\par\n\lean{ComputableAnalysis.RealRaw.mul}\par}\n\uses{def:raw-real,def:raw-real-sign}\nLet \(x\) and \(y\) be real numbers:\n\[\n  x=[a_n,b_n]_{n\in\N},\qquad y=[c_n,d_n]_{n\in\N}.\n\]\nTheir sum is defined stagewise:\n\[\n  x+y := [a_n+c_n,b_n+d_n]_{n\in\N},\n\]\nNegation of \([c_n,d_n]\) is given by \([-d_n,-c_n]\), so the difference is\n\[\n  x-y := [a_n-d_n,b_n-c_n]_{n\in\N},\n\]\nThe product is defined by taking the bounding interval of all four\nproducts:\n\[\n  xy:=\n  \left[\n    \min S_n,\max S_n\n  \right]_{n\in\N},\n  \qquad\n  S_n=\{a_nc_n,a_nd_n,b_nc_n,b_nd_n\}.\n\]\nAn inverse needs an explicit apartness schedule: from a stage at which\n\([a_N,b_N]\) avoids zero, the intended later construction is\n\[\n  x^{-1}:= [ b_{N+n}^{-1}, a_{N+n}^{-1} ]_{n\in\N}.\n\]\nThe current formal layer records this as a future computable-inverse\nobligation rather than claiming a completed general inverse or division\noperation.\n\end{definition}\n\n\n\begin{proposition}[Arithmetic preserves real numbers]\label{prop:raw-arithmetic-valid}\n{\footnotesize\raggedright\n\lean{ComputableAnalysis.QBox.mulRealInterval_of_nonneg}\par\n\lean{ComputableAnalysis.RealRaw.mul_valid_of_nonneg_bounded}\par\n\lean{ComputableAnalysis.RealRaw.le_mul_le_mul_of_nonneg}\par\n\lean{ComputableAnalysis.RealRaw.mul_equiv_of_nonneg}\par\n\lean{ComputableAnalysis.RealRaw.scaleRat_valid_of_nonneg}\par\n\lean{ComputableAnalysis.RealRaw.valid_of_scaleRat_valid_of_pos}\par\n\lean{ComputableAnalysis.RealRaw.valid_of_natScale_valid}\par\n\lean{ComputableAnalysis.RealRaw.add_width}\par\n\lean{ComputableAnalysis.RealRaw.sub_width}\par\n\lean{ComputableAnalysis.RealRaw.natScale_width}\par\n\lean{ComputableAnalysis.RealRaw.scaleRat_equiv_of_nonneg}\par\n\lean{ComputableAnalysis.RealRaw.equiv_of_scaleRat_equiv_of_pos}\par\n\lean{ComputableAnalysis.RealRaw.equiv_of_natScale_equiv}\par}\n\uses{def:raw-real,def:raw-real-arithmetic}\nAddition, subtraction, negation, and rational scaling preserve valid raw\nreals.  The first checked multiplication theorem is deliberately local and\nquantitative: if both factors have nonnegative interval enclosures and a\npositive rational upper bound, their four-corner product enclosure is valid.\nAt stage \(n\) its width is bounded by\n\[\n B_x\operatorname{width}(y_n)+B_y\operatorname{width}(x_n),\n\]\nso widths shrink by an explicit rational precision budget.  The same positive\nproduct computation respects proved raw equivalence.  This is the product\ncase needed on positive bounded branches such as \([0,1]\); the fully\nsign-sensitive multiplication and an inverse construction remain separate\nfoundational work, rather than consequences of an ambient completed field.\nPositive rational scaling is reversible at the level of validity and\nequivalence: if \(r>0\), then validity of \(rx\) implies validity of \(x\),\nand \(rx\sim ry\) implies \(x\sim y\).\nAt each finite stage, addition and subtraction add interval widths exactly,\nand natural scaling multiplies them by the corresponding natural number; these\nfinite identities support explicit precision budgets for composite\ncomputations.\n\end{proposition}\n\n\begin{definition}[Equivalence of real numbers]\label{def:raw-equiv}\n\lean{ComputableAnalysis.RealRaw.Equiv}\n\uses{def:raw-real}\nTwo real numbers\n\[\n  x=[a_n,b_n]_{n\in\N},\qquad y=[c_n,d_n]_{n\in\N}\n\]\nare \emph{equivalent} if they overlap at every stage, i.e.,\n\[\n  [a_n,b_n] \mbox{ overlaps with } [c_n,d_n]\n  \qquad\text{for all }n.\n\]\n\end{definition}\n\n\begin{proposition}[Equivalence relation]\label{prop:raw-equiv-relation}\n\lean{ComputableAnalysis.RealRaw.equiv_refl}\n\lean{ComputableAnalysis.RealRaw.equiv_symm}\n\lean{ComputableAnalysis.RealRaw.equiv_trans}\n\uses{def:raw-real,def:raw-equiv}\nEquivalence of real numbers is an equivalence relation.\n\end{proposition}\n\n\begin{proposition}[Raw-real order compatibility]\n\lean{ComputableAnalysis.RealRaw.le_add_le_add}\n\lean{ComputableAnalysis.RealRaw.le_neg_le_neg}\n\lean{ComputableAnalysis.RealRaw.le_sub_le_sub}\n\lean{ComputableAnalysis.RealRaw.le_antisymm}\n\lean{ComputableAnalysis.RealRaw.le_scaleRat_le_scaleRat}\n\lean{ComputableAnalysis.RealRaw.le_scaleRat_le_scaleRat_of_nonpos}\nThe interval-defined raw-real order is preserved by addition and subtraction,\nand by\nnonnegative rational scaling, and reverses under nonpositive scaling.  These\nare finite endpoint inequalities on the underlying algorithms; no\ncompleted-real order or completeness principle is used.\n\end{proposition}\n\n\begin{proposition}[Finite-prefix interval stabilization]\n\label{prop:raw-prefix-stabilization}\n{\footnotesize\raggedright\n\lean{ComputableAnalysis.RealRaw.prefixStabilize}\par\n\lean{ComputableAnalysis.RealRaw.prefixStabilize_valid}\par}\n\uses{def:raw-real,def:raw-equiv}\nSuppose a direct interval algorithm $C_n$ overlaps a valid anchor algorithm\n$A_n$ at every stage, both have explicit rational width moduli, and a public\nrational radius $r_n$ satisfies $\operatorname{width}(A_n)\le r_n$ and\n$r_n\to0$.  The stabilized algorithm takes the finite intersection\n\n\[\n  \bigcap_{k=0}^{n} [C_k^- - r_k,\;C_k^+ + r_k].\n\]\n\nIt is a valid raw real and overlaps both $C$ and $A$.  Evaluation reads\nonly the direct intervals and the public radii; the anchor appears only in the\nfinite rational proof of the enclosure.  Thus this construction packages a\nknown error bound into a nested evaluator without invoking a limit theorem or\ncompleteness.\n\end{proposition}\n\n\begin{definition}[Stage schedules]\label{def:stage-schedule}\n\lean{ComputableAnalysis.RealRaw.StageSchedule}\n\lean{ComputableAnalysis.RealRaw.schedule}\n\uses{def:raw-real,def:raw-equiv}\nA \emph{stage schedule} is an increasing function\n\[\n  \sigma:\N\to\N.\n\]\nIf \(x=[a_n,b_n]_{n\in\N}\), then the scheduled real number is\n\[\n  \sigma\ldot x:=[a_{\sigma(n)},b_{\sigma(n)}]_{n\in\N}.\n\]\n\end{definition}\n\n\begin{proposition}[Using stage schedules]\label{prop:stage-schedule-equiv}\n\lean{ComputableAnalysis.RealRaw.schedule_equiv}\n\lean{ComputableAnalysis.RealRaw.equiv_of_schedule_equiv}\n\uses{def:stage-schedule,def:raw-equiv}\nFor every stage schedule \(\sigma\), one has\n\[\n  x\sim \sigma\ldot x.\n\]\nThus, to prove \(x\sim y\), it is enough to choose schedules\n\(\sigma,\tau\) that make\n\[\n  \sigma\ldot x\sim \tau\ldot y\n\]\neasier to show.\n\end{proposition}\n\n\begin{definition}[Certified real handles]\label{def:reals-quotient}\n\lean{ComputableAnalysis.Real}\n\uses{def:raw-real,def:raw-equiv}\nThe current Lean-facing real type is a \emph{certified handle}, not a quotient\nor a completed field.  It stores one preferred valid raw interval algorithm\nand a finite list of alternative valid algorithms, each equipped with a proof\nof \(\Equiv\) to the preferred one.  This makes a chosen computation and its\nrate directly available while recording the representation changes that have\nalready been proved.\nIn particular, \code{Real.Representation.equiv} proves that any two certified\nrepresentations of the same handle have equivalent raw evaluators.  This is an\ninteroperability theorem about proved representations, not an identification\nof all raw algorithms by quotienting.\nFor functions, the same distinction is kept at the appropriate level: named\npresentations use dot-qualified public names.  Thus \code{arctan.geom} and\n\code{arctan.series} are explicit domain-aware function representations, while\nthe later integral and rectangle calculators are denoted\n\code{arctan.integral} and \code{arctan.integral.rectangle}.  At a certified rational\ninput their raw outputs may be packaged as \code{Real} handles, and a proved\npointwise agreement is then recorded with \code{Real.withAlternative}; no\nquotient or unproved global identification is introduced.\n\end{definition}\n\n\begin{proposition}[Raw arithmetic respects representations]\label{thm:raw-reals-field}\n\lean{ComputableAnalysis.Real.ofRat}\n\lean{ComputableAnalysis.RealRaw.add_equiv}\n\lean{ComputableAnalysis.RealRaw.sub_equiv}\n\lean{ComputableAnalysis.RealRaw.mul_equiv_of_nonneg}\n\uses{def:reals-quotient,def:raw-real-sign,def:raw-real-arithmetic,prop:raw-arithmetic-valid}\nThe checked arithmetic operations preserve validity and are compatible with\nproved raw equivalence in their stated domains; in particular, the new\nnonnegative bounded product does not select a preferred representative.\nConstant rational intervals provide certified handles through\n\code{Real.ofRat}.  The development deliberately does not assert that these\nhandles form a quotient, an ordered field, or a completed continuum; later\ntheorems remain stated in terms of valid raw algorithms and explicit\nequivalence certificates.\n\end{proposition}\n\n\begin{remark}[No ambient completeness theorem]\label{rem:real-completeness}\n\uses{thm:raw-reals-field}\nNo theorem identifies the raw interval representation with classical Cauchy\nreals or derives completeness.  When a proof needs existence or convergence,\nit constructs the relevant rational interval algorithm and proves orderedness,\nnesting, and an explicit width-shrinking bound directly.\n\end{remark}\n\n\section{Examples of real numbers \humanchecked}\n\n\subsection{Benchmark foundations}\label{sec:benchmark-foundations}\n\nThe rational foundation already contains the exact triangle inequality for\nthe project's absolute-value operation:\n\begin{theorem}[Rational triangle-inequality certificate]\n\label{thm:rational-triangle-inequality}
\label{thm:finite-triangle-inequality-interface}
The rational absolute-value inequality is the reusable finite interface for
error budgets; completed metric-space formulations are separate bridges.\n\lean{ComputableAnalysis.qabs_add_le}\n\lean{ComputableAnalysis.qabs_add_le_three}\n\lean{ComputableAnalysis.qabs_three_term_triangle_certificate}\n\lean{ComputableAnalysis.qabs_five_term_triangle_certificate}\n\lean{ComputableAnalysis.qabs_perturbed_sub_le}\n\lean{ComputableAnalysis.complexTriangleZ}\n\lean{ComputableAnalysis.complexTriangleW}\n\lean{ComputableAnalysis.complexTriangle_norms}\n\lean{ComputableAnalysis.complexTriangle_squared_norm_bound}\n\lean{ComputableAnalysis.complexTriangle_certificate}\n\lean{ComputableAnalysis.rational_coordinate_cauchy_schwarz}\n\lean{ComputableAnalysis.qcomplex_coordinate_cauchy_schwarz}\n\lean{ComputableAnalysis.qcomplex_normSq_add_expansion}\n\lean{ComputableAnalysis.qcomplex_normSq_add_le_coordinate_abs}\n\lean{ComputableAnalysis.ratListSum}\n\lean{ComputableAnalysis.ratListAbsSum}\n\lean{ComputableAnalysis.ratListSum_append}\n\lean{ComputableAnalysis.ratListAbsSum_append}\n\lean{ComputableAnalysis.qabs_ratListSum_le}\n\lean{ComputableAnalysis.qabs_sub_le}\n\lean{ComputableAnalysis.QInterval.intersection_ordered_of_overlaps}\n\leanok\nThe three-term form is included explicitly for chained interval-width and\nfinite error estimates; these are the rational certificate core of benchmark\nitem 91.  The list-level theorem `qabs\_ratListSum_le` extends the\nsame estimate to every finite rational error list.\nThe five-term worked list ((-3,4,-2,7,-5)) has sum (1), absolute-value\nsum (21), and satisfies the same list-level triangle certificate.\nThe append laws make these finite certificates compositional.\nThese finite absolute-value and list-sum bounds are the project's constructive\ntriangle-inequality interface for interval widths and error budgets.\nThe perturbation form \texttt{qabs\_perturbed\_sub\_le} makes the error-budget\nuse explicit: replacing \(x,y\) by \(x+e,y+f\) adds at most the certified\nrational error bounds for \(e\) and \(f\) to the original separation.\nThe worked rational-complex certificate adds vectors ((3,4)) and ((5,12)):\ntheir squared norms are (25) and (169), the squared norm of their sum is\n(320), and (320le(5+13)^2).\nThe reusable coordinate Cauchy--Schwarz theorem is proved by the explicit\nLagrange remainder `(a*d-b*c)^2`.  Its `QComplex` specialization bounds the\ndot product by the product of squared norms, and the accompanying expansion\nof the squared norm of a sum exposes the finite algebra used by later norm\nand triangle certificates.\n\end{theorem}\n\n\begin{theorem}[Rational AM--GM square certificate]\n\label{thm:rational-am-gm}
\label{thm:finite-am-gm-interface}
\lean{ComputableAnalysis.FiniteAMGMCertificate}
\lean{ComputableAnalysis.FiniteAMGMCertificate.product_le_average_pow}
\lean{ComputableAnalysis.FiniteAMGMCertificate.product_mul_card_pow_le_sum_pow}\n\lean{ComputableAnalysis.rat_square_nonneg_basic}\n\lean{ComputableAnalysis.am_gm_square_bound}\n\lean{ComputableAnalysis.am_gm_rational_half}\n\lean{ComputableAnalysis.am_gm_rational_half_eq_iff}\n\lean{ComputableAnalysis.am_gm_four}\n\lean{ComputableAnalysis.amgm_2_8_certificate}\n\lean{ComputableAnalysis.amgm_3_3_equality}\n\lean{ComputableAnalysis.amgm_2_8_and_equality_certificate}\n\lean{ComputableAnalysis.amgm_four_equal_certificate}\n\lean{ComputableAnalysis.amgm_four_strict_certificate}\n\lean{ComputableAnalysis.amgm_four_certificate}\n\lean{ComputableAnalysis.DyadicAMGM}\n\lean{ComputableAnalysis.DyadicAMGM.sum}\n\lean{ComputableAnalysis.DyadicAMGM.product}\n\lean{ComputableAnalysis.DyadicAMGM.product_le_average_pow}\n\lean{ComputableAnalysis.DyadicAMGM.product_mul_card_pow_le_sum_pow}\n\lean{ComputableAnalysis.dyadicAMGMExample}\n\lean{ComputableAnalysis.dyadicAMGMExample_sum}\n\lean{ComputableAnalysis.dyadicAMGMExample_product}\n\lean{ComputableAnalysis.dyadicAMGMExample_certificate}\n\lean{ComputableAnalysis.dyadicAMGMExample_numeric_bound}\n\lean{ComputableAnalysis.dyadicAMGMExampleDepthThree}\n\lean{ComputableAnalysis.dyadicAMGMExampleDepthThree_sum}\n\lean{ComputableAnalysis.dyadicAMGMExampleDepthThree_product}\n\lean{ComputableAnalysis.dyadicAMGMExampleDepthThree_certificate}\n\lean{ComputableAnalysis.dyadicAMGMExampleDepthThree_numeric_bound}\n\lean{ComputableAnalysis.dyadicAMGMExampleDepthFour}\n\lean{ComputableAnalysis.dyadicAMGMExampleDepthFour_sum}\n\lean{ComputableAnalysis.dyadicAMGMExampleDepthFour_product}\n\lean{ComputableAnalysis.dyadicAMGMExampleDepthFour_certificate}\n\lean{ComputableAnalysis.dyadicAMGMExampleDepthFour_numeric_bound}\n\lean{ComputableAnalysis.consecutiveDyadic}\n\lean{ComputableAnalysis.dyadicAMGMExampleDepthFive}\n\lean{ComputableAnalysis.dyadicAMGMExampleDepthFive_sum}\n\lean{ComputableAnalysis.dyadicAMGMExampleDepthFive_product}\n\lean{ComputableAnalysis.dyadicAMGMExampleDepthFive_certificate}\n\lean{ComputableAnalysis.dyadicAMGMExampleDepthFive_numeric_bound}\n\lean{ComputableAnalysis.sqrtRaw_le_am_gm}\n\lean{ComputableAnalysis.sqrtRaw_am_gm_eq_of_eq}\n\lean{ComputableAnalysis.sqrtRaw_am_gm_eq_iff}\n\leanok\nThe square bound and paired four-variable certificate are the exact rational\ncore of benchmark 38 (AM--GM).  The certified square-root interface lifts the\nbound to the project's represented nonnegative values, and the equality\ncertificate records that equality occurs exactly when the two inputs coincide.\nThe direct four-variable witnesses check equality for \((3,3,3,3)\) and a\nstrict inequality for \((2,3,4,5)\), making the paired four-variable interface\nexecutable on both equality and non-equality data.\nThe depth-three worked tree has leaves (1,\ldots,8), sum (36), product\n(40320), and satisfies the corresponding eight-term dyadic bound.\nThe depth-four worked tree has leaves (1,\ldots,16), sum (136), product\n(20922789888000), and satisfies the corresponding sixteen-term dyadic bound.\nThe generic finite constructor `consecutiveDyadic` now produces the depth-five\ntree with leaves (1,\ldots,32), sum (528), product\n(263130836933693530167218012160000000), and its corresponding thirty-two-term bound.\nThe worked depth-two tree with leaves (1,2,3,4) has sum (10), product\n(24), and satisfies the generic bound (24le(10/4)^4).  This is an\nexecutable finite witness for the recursive dyadic interface.\n\end{theorem}\n\n\begin{theorem}[Finite rational Cauchy--Schwarz certificates]\n\label{thm:rational-cauchy-schwarz}
\label{thm:finite-cauchy-schwarz-interface}
\lean{ComputableAnalysis.FiniteCauchySchwarzCertificate}
\lean{ComputableAnalysis.FiniteCauchySchwarzCertificate.inequality}
\lean{ComputableAnalysis.FiniteCauchySchwarzCertificate.square_sums_nonneg}
The list-level theorem is the finite project interface: dot products and sums
of squares are rational folds, and the residual is a sum of squared minors.\n\lean{ComputableAnalysis.cauchy_schwarz_2d}\n\lean{ComputableAnalysis.cauchy_schwarz_2d_eq_iff}\n\lean{ComputableAnalysis.cauchy_schwarz_3d}\n\lean{ComputableAnalysis.cauchy_schwarz_3d_eq_iff}\n\lean{ComputableAnalysis.cauchy_schwarz_4d}\n\lean{ComputableAnalysis.cauchy_schwarz_4d_eq_iff}\n\lean{ComputableAnalysis.cauchy_schwarz_5d}\n\lean{ComputableAnalysis.cauchy_schwarz_5d_eq_iff}\n\lean{ComputableAnalysis.cauchy_schwarz_6d}\n\lean{ComputableAnalysis.cauchy_schwarz_6d_eq_of_minors}\n\lean{ComputableAnalysis.rationalDot}\n\lean{ComputableAnalysis.rationalSumSquares}\n\lean{ComputableAnalysis.rationalMinorSquareSum}\n\lean{ComputableAnalysis.rationalDot_cauchy_schwarz_of_length_eq}\n\lean{ComputableAnalysis.cauchy_schwarz_2d_example_equality}\n\lean{ComputableAnalysis.cauchy_schwarz_2d_example_proportional}\n\lean{ComputableAnalysis.cauchy_schwarz_2d_example_certificate}\n\lean{ComputableAnalysis.cauchyThreeXs}\n\lean{ComputableAnalysis.cauchyThreeYs}\n\lean{ComputableAnalysis.cauchyThree_dot}\n\lean{ComputableAnalysis.cauchyThree_sum_squares}\n\lean{ComputableAnalysis.cauchyThree_minor_residual}\n\lean{ComputableAnalysis.cauchyThree_certificate}\n\lean{ComputableAnalysis.cauchyFourXs}\n\lean{ComputableAnalysis.cauchyFourYs}\n\lean{ComputableAnalysis.cauchyFour_dot}\n\lean{ComputableAnalysis.cauchyFour_sum_squares}\n\lean{ComputableAnalysis.cauchyFour_minor_residual}\n\lean{ComputableAnalysis.cauchyFour_certificate}\n\lean{ComputableAnalysis.cauchySixXs}\n\lean{ComputableAnalysis.cauchySixYs}\n\lean{ComputableAnalysis.cauchySix_dot}\n\lean{ComputableAnalysis.cauchySix_sum_squares}\n\lean{ComputableAnalysis.cauchySix_minor_residual}\n\lean{ComputableAnalysis.cauchySix_certificate}\n\leanok\nThese finite rational Cauchy--Schwarz certificates cover benchmark 78 as\nsupporting inequality infrastructure, without introducing a functional-analysis\nchapter.  The list-based certificate gives the same inequality for every\nfinite equal-length rational vector, using an explicit sum of squared minors.\nThe worked four-coordinate example uses ((1,2,3,4)) and ((4,3,2,1)):\nboth squared norms are (30), the dot product is (20), and the residual is\n(500).\nThe six-coordinate example uses ((1,2,3,4,5,6)) and ((6,5,4,3,2,1)):\nboth squared norms are (91), the dot product is (56), and the residual is\n(5145).\n\end{theorem}\n\nMathematical induction is the recursion\nprinciple used to check\nthese finite identities and the series identities in Chapter~\ref{ch:infinite-series};\nit is not reintroduced as a separate completed-real theorem.\nThe public schema is\n\lean{ComputableAnalysis.nat_induction_schema}.\n\n\begin{theorem}[Executable Euclidean gcd]\n\label{thm:euclidean-gcd}
\emph{GCD interface.} Run the remainder loop to termination and retain the Bézout witness.
\begin{verbatim}
(g,d) := EuclideanRemainderLoop(a,b)
assert d | a /\ d | b
assert u*a + v*b = d
\end{verbatim}
\lean{ComputableAnalysis.FiniteEuclideanGcdCertificate}
\lean{ComputableAnalysis.FiniteEuclideanGcdCertificate.divides_left}
\lean{ComputableAnalysis.FiniteEuclideanGcdCertificate.divides_right}
\lean{ComputableAnalysis.FiniteEuclideanGcdCertificate.is_coprime_of_value_one}
\lean{ComputableAnalysis.finiteEuclideanGcdCertificate}\n\lean{ComputableAnalysis.euclideanGcd}\n\lean{ComputableAnalysis.euclideanGcd_eq_gcd}\n\lean{ComputableAnalysis.euclideanGcd_step}\n\lean{ComputableAnalysis.euclideanGcd_zero_right}\n\lean{ComputableAnalysis.euclideanGcd_zero_left}\n\lean{ComputableAnalysis.euclideanGcd_pos_of_pos}\n\lean{ComputableAnalysis.euclideanGcd_pos_iff}\n\lean{ComputableAnalysis.euclideanGcd_eq_one_iff_coprime}\n\lean{ComputableAnalysis.euclideanGcd_comm}\n\lean{ComputableAnalysis.euclideanGcd_dvd_left}\n\lean{ComputableAnalysis.euclideanGcd_dvd_right}\n\lean{ComputableAnalysis.euclideanGcd_dvd_of_dvd}\n\lean{ComputableAnalysis.euclideanGcd_dvd_iff}\n\lean{ComputableAnalysis.gcd_remainder_swap}\n\lean{Nat.gcd}\n\lean{Nat.gcd_rec}\n\lean{Nat.gcd_dvd_left}\n\lean{Nat.gcd_dvd_right}\n\lean{Nat.dvd_gcd}\n\leanok\nThe executable loop is definitionally the usual remainder descent, and its\nequality with the core gcd is checked by induction on the second argument.\nThe exact natural-number Euclidean primitive is also available from the Lean\ncore and is used by the project's discrete modules.  These declarations provide\nthe algorithmic gcd milestone (benchmark 69).\n\end{theorem}\n\n\begin{theorem}[Executable Bézout certificate]\n\label{thm:bezout-certificate}\n\lean{ComputableAnalysis.bezout_step_identity}\n\lean{ComputableAnalysis.bezout_exists}\n\lean{ComputableAnalysis.euclideanGcd_bezout_exists}\n\lean{ComputableAnalysis.euclideanGcd_84_30}\n\lean{ComputableAnalysis.bezout_84_30}\n\lean{ComputableAnalysis.bezout_84_30_certificate}\n\lean{ComputableAnalysis.euclideanGcd_252_198}\n\lean{ComputableAnalysis.bezout_252_198}\n\lean{ComputableAnalysis.bezout_252_198_certificate}\n\lean{ComputableAnalysis.euclideanGcd_1071_462}\n\lean{ComputableAnalysis.bezout_1071_462}\n\lean{ComputableAnalysis.bezout_1071_462_certificate}\n\lean{ComputableAnalysis.euclideanGcd_12345_6789}\n\lean{ComputableAnalysis.bezout_12345_6789}\n\lean{ComputableAnalysis.bezout_12345_6789_certificate}\n\lean{ComputableAnalysis.euclideanGcd_99991_12345}\n\lean{ComputableAnalysis.bezout_99991_12345}\n\lean{ComputableAnalysis.bezout_99991_12345_certificate}\n\leanok\nThe recursive coefficient certificate \texttt{bezout\_exists} proves the signed\nBézout identity for every natural input pair, covering benchmark item 60 in the\nexact-arithmetic representation.  The combined theorem\n\texttt{euclideanGcd\_bezout\_exists} identifies those coefficients with the\nexecutable Euclidean-loop result itself.\nThe worked input pair (84,30) gives (gcd(84,30)=6) with the explicit\ncertificate ((-1)84+3(30)=6).\nThe pair (99991,12345) supplies a further terminating certificate: its gcd is\n(1), witnessed by \(2116\cdot99991-17139\cdot12345=1\).\n\end{theorem}\n\n\begin{remark}[Algebraic numbers, starting with \(\sqrt2\)]\n\label{rem:algebraic-numbers-sqrt-two}\n\lean{ComputableAnalysis.sqrt}\n\lean{ComputableAnalysis.sqrtRaw}\n\lean{ComputableAnalysis.sqrtStageEps}\n\lean{ComputableAnalysis.one_div_natCast_den_eq}\n\lean{ComputableAnalysis.sqrtStageEps_den_eq}\n\lean{ComputableAnalysis.sqrtFuel_sqrtStageEps_eq}\n\lean{ComputableAnalysis.sqrtStageEps_den_mono}\n\lean{ComputableAnalysis.sqrtFuel_sqrtStageEps_mono}\n\lean{ComputableAnalysis.sqrtBisectStep_width_eq_half}\n\lean{ComputableAnalysis.sqrtBisect_width_eq}\n\lean{ComputableAnalysis.sqrtApproxOnDomain_width_eq}\n\lean{ComputableAnalysis.sqrtApproxOnDomain_width_eq_positive_stage}\n\lean{ComputableAnalysis.sqrtBisectStep_contains}\n\lean{ComputableAnalysis.sqrtBisect_contains_of_le_fuel}\n\lean{ComputableAnalysis.sqrtApproxOnDomain_width_le_nat_over_succ}\n\lean{ComputableAnalysis.sqrtApproxOnDomain_valid}\n\lean{ComputableAnalysis.sqrtRaw_valid}\n\lean{ComputableAnalysis.sqrtRaw_spec}\n\lean{ComputableAnalysis.sqrtCertified_of_domain}\n\lean{ComputableAnalysis.sqrtRat}\n\lean{ComputableAnalysis.irrational_sqrt_ratCast_iff_of_nonneg}\n\lean{ComputableAnalysis.irrational_sqrt_rat_iff_not_square}\n\lean{ComputableAnalysis.sqrtTwo_descent_core}\n\lean{ComputableAnalysis.two_not_rat_square_via_descent}\n\lean{ComputableAnalysis.sqrt_two_irrational_via_descent}\n\lean{ComputableAnalysis.sqrt_rational_iff_square}\n\lean{ComputableAnalysis.sqrt_rational_iff_lowest_terms_square}\n\lean{ComputableAnalysis.sqrt_irrational_iff_not_square}\n\lean{ComputableAnalysis.sqrt_irrational_iff_lowest_terms_nonsquare}\n\lean{ComputableAnalysis.sqrt_two_irrational}\n\lean{ComputableAnalysis.sqrtTwo_bisection_stage4}\n\lean{ComputableAnalysis.sqrtTwo_bisection_stage4_bracket}\n\lean{ComputableAnalysis.sqrtTwo_bisection_stage4_width}\n\lean{ComputableAnalysis.sqrtTwo_bisection_stage8}\n\lean{ComputableAnalysis.sqrtTwo_bisection_stage8_bracket}\n\lean{ComputableAnalysis.sqrtTwo_bisection_stage8_width}\n\lean{ComputableAnalysis.sqrtTwo_bisection_stage12}\n\lean{ComputableAnalysis.sqrtTwo_bisection_stage12_bracket}\n\lean{ComputableAnalysis.sqrtTwo_bisection_stage12_width}\n\lean{ComputableAnalysis.sqrtTwo_bisection_stage16}\n\lean{ComputableAnalysis.sqrtTwo_bisection_stage16_bracket}\n\lean{ComputableAnalysis.sqrtTwo_bisection_stage16_width}\n\lean{ComputableAnalysis.sqrtTwo_bisection_stage20}\n\lean{ComputableAnalysis.sqrtTwo_bisection_stage20_bracket}\n\lean{ComputableAnalysis.sqrtTwo_bisection_stage20_width}\n\lean{ComputableAnalysis.sqrtTwo_bisection_stage24}\n\lean{ComputableAnalysis.sqrtTwo_bisection_stage24_bracket}\n\lean{ComputableAnalysis.sqrtTwo_bisection_stage24_width}\n\uses{def:raw-real}\nThe first nontrivial computable real number to keep in mind is \(\sqrt2\).\nFor \(\sqrt2\), use rational secant--tangent updates:\n\begin{verbatim}\ndef sqrt2(n):\n    a = 1\n    b = 2\n    for _ in range(n):\n        a = (2 + a*b) / (a + b)\n        b = (b + 2/b) / 2\n    return [a, b]\n\end{verbatim}\n\begin{figure}[ht]\n\centering\n\sqrtSecantTangentAnimation\n\caption{Secant--tangent rational enclosure for \(\sqrt2\).}\n\label{fig:sqrt-secant-tangent}\n\end{figure}\nThe named theorem `sqrt_two_irrational_via_descent` instantiates the general\ncriterion at the benchmark value $2$.  Its nonsquare input is connected to the\nclassical infinite-descent kernel `sqrtTwo_descent_core`, so the historical\nproof is visible while the resulting irrationality statement remains entirely\nover the project's `RealRaw` representation.\nThe companion theorem `sqrtTwo_bisection_stage4` gives the explicit stage-four\nenclosure $[11/8,23/16]$ for the target equation $x^2=2$, with width $1/16$.\nIts bracket certificate records the two finite rational comparisons directly.\nAt stage eight the same evaluator returns the tighter interval\n([181/128,363/256]), whose width is (1/256); this is another finite\ncomparison checkpoint rather than an attained square root.\nAt stage twelve the interval is ([181/128,5793/4096]), with width\n(1/4096), giving a still finer finite enclosure.\nAt stage sixteen it is ([92681/65536,46341/32768]), with width\n(1/65536), extending the exact finite precision schedule.\nAt stage twenty it is ([741455/524288,1482911/1048576]), with width\n(1/1048576), extending the same finite schedule without treating the limit as\nan attained endpoint.\nAt stage twenty-four it is\n([11863283/8388608,23726567/16777216]), with width (1/16777216),\ncontinuing the exact rational bisection schedule.\nAlgebraic numbers are represented by rational isolating intervals.  Lean\nproves this computation's nesting, explicit width schedule, irrationality\ncriterion, and unit-interval rational \(\varepsilon\)--\(\delta\) modulus.\n\end{remark}\n\n\begin{remark}[Pseudocode convention]\label{rem:pseudocode-convention}\n\uses{rem:computable-presentations,rem:algebraic-numbers-sqrt-two}\nDisplayed code is Python-like finite rational pseudocode.  Variables are\nrational unless stated otherwise; no floating-point operation is implied.  The\nlinked Lean declarations supply the certificates.\n\end{remark}\n\n\begin{remark}[Other types of real numbers]\label{rem:sources-of-raw-reals}\n\uses{def:interval-sequence,def:raw-real,rem:algebraic-numbers-sqrt-two}\nBeyond algebraic numbers, there are other useful ways of defining real\nnumbers.  Most of the non-algebraic examples in this project fall into one of\nthe following types:\n\begin{itemize}\n  \item Infinite series, for example,\n  \[\n    \frac{\pi}{4}\n    =\n    1-\frac{1}{3}+\frac{1}{5}-\frac{1}{7}+\cdots\n  \]\n  and\n  \[\n    \frac{\pi^2}{6}\n    =\n    1+\frac{1}{4}+\frac{1}{9}+\frac{1}{16}+\cdots .\n  \]\n  of Leibniz and Euler. These are easy to implement, though the latter requires a little\n  care with the upper bound (hint: telescoping sum).\n\n  \item Continued fractions, such as\n  \[\n    \frac{4}{\pi}\n    =\n    1+\cfrac{1^2}{2+\cfrac{3^2}{2+\cfrac{5^2}{2+\cfrac{7^2}{2+\ddots}}}}.\n  \]\n  and finite truncations work as rational bounds.\n\n  \item Periods, i.e., integrals of rational or algebraic functions:\n  \[\n    \frac{\pi}{4}\n    =\n    \int_0^1\frac{\dd x}{1+x^2},\n  \]\n  \[\n    \frac{\pi}{6}\n    =\n    \int_0^{1/2}\frac{\dd x}{\sqrt{1-x^2}},\n  \]\n  \[\n    \frac{\pi}{12}+\frac{\sqrt3}{8}\n    =\n    \int_0^{1/2}\sqrt{1-x^2}\,\dd x.\n  \]\n  and upper and lower Riemann sums work. (The last one is what Newton used to compute \(\pi\),\n  with binomial expansion.)\n\n  \item Integrals (proper or not) of transcendental functions are also\n  likely in play, for instance\n  \[\n    \sqrt{2\pi}\n    =\n    \int_{-\infty}^{\infty} e^{-x^2/2}\,\dd x.\n  \]\n\end{itemize}\n\nOne may define \(\pi\) by one of these formulas, and the others would be\ntheorems (of equivalence of two real numbers). Or perhaps one should\ndefine \(\pi\) by Archimedes' method of exhaustion.\n\end{remark}\n\n\begin{remark}[Newton and the space of computations]\n\label{rem:newton-space-of-computations}\nThe general lesson is not that one should work directly toward the best\napproximation of $\pi$.  Since Archimedes, many computations of area were\navailable: polygonal exhaustions, rectangle sums, series, continued\nfractions, and integral representations.  A breakthrough can come from\nunderstanding this space of computations, fast and slow alike, and the\nrelations that connect one computation to another, rather than from directly\noptimizing the original procedure.\n\nThe Newton-era route from binomial series and term-by-term integration to a\nrapidly convergent series for $\pi$ is a useful mathematical example of this\npattern; it soon surpassed the speed of direct Archimedean exhaustion.  This\nremark makes no claim about Newton's private reasoning or the exact order of\nhis discoveries.  It extracts a broader methodological principle: progress\noften comes from moving through equivalent representations until a new\nconnection reveals a better computation.\n\nThis is the methodological model for the present project.  We formalize\nindividual algorithms together with their validity, rates, common enclosures,\nand equivalence maps.  A slow rectangle computation remains mathematically\nvaluable because it can be the bridge to a faster series or integral\ncomputation.\n\end{remark}\n\n\begin{remark}[Calculus as equivalence]\label{rem:calculus-as-equivalence}\n\uses{def:raw-equiv,rem:sources-of-raw-reals}\nA great deal of calculus is the art of proving that independently constructed\nreal numbers are equivalent. The great advance by Newton and Leibniz is\nto turn that art mechanical, by leveraging the fundamental formula\n\[\n  \int_a^b f'(x)\,\dd x = f(b)-f(a)\n\]\nwhich we shall establish, \emph{without} the completeness of real numbers.\n\end{remark}\n\n\begin{remark}[Algorithm with a variable]\label{rem:parameterized-constructions}\n\uses{rem:sources-of-raw-reals}\nIn many of these algorithms, there is a natural variable that could assume\ndifferent values (\(\sqrt{\phantom{x}}\), \(\arctan\), \(\arcsin\) in the\nexamples above), so it makes sense to define the algorithms\n\[\n  n,t\longmapsto [a_n(t),b_n(t)]\n\]\nto be (computable) \emph{functions}. However, not all rational values of \(t\)\nneed to result in a real number.\n\nThis turns out to be crucial for establishing many of the equivalences of real\nnumbers, and is how calculus appears to be the study of functions.\n\end{remark}\n\n\begin{remark}[Complex variable]\label{rem:complex-parameters}\n\uses{rem:parameterized-constructions,def:qcomplex,def:qbox}\nIt turns out that often it makes sense for the variable to assume complex values\n(at least for \(t+si\), with \(t,s\in\Q\)).  For example, power series may be evaluated\ninside a disk in the complex plane, and integrals can be taken over paths in the complex plane.\n\end{remark}\n\n\section{Complex numbers \humanchecked}\n\nOne could define complex numbers as pairs of real numbers.  Instead, we follow\nthe interval arithmetic more closely, using rectangular boxes with rational corners\nfrom the start.\n\n\begin{definition}[Field \(\Q(i)\)]\label{def:qcomplex}\n\lean{ComputableAnalysis.QComplex}\nLet\n\[\n  \Q(i):=\{r+is:r,s\in\Q\},\n\]\nwith the standard field operations:\n\[\n  (r+is)(u+iv)=(ru-sv)+i(rv+su),\n\]\netc. We also use the coordinatewise (partial) order:\n\[\n  r+is \le u+iv\n  \quad\Longleftrightarrow\quad\n  r\le u\ \text{and}\ s\le v.\n\]\nNote that this is not a total/linear order on \(\Q(i)\).\n\end{definition}\n\n\begin{definition}[Rational box]\label{def:qbox}\n\lean{ComputableAnalysis.QBox}\n\uses{def:qcomplex}\nA \emph{rational box} is a pair\n\[\n  \cbox{a}{b}\in \Q(i)\times\Q(i),\n  \qquad a\le b.\n\]\nTwo boxes \(\cbox{a}{b}\) and\n\(\cbox{c}{d}\) \emph{overlap} if\n\[\n  a\le d\ \text{and}\ c\le b.\n\]\n\end{definition}\n\n\begin{definition}[Box sequence]\label{def:box-sequence}\n\uses{def:qbox}\nA \emph{box sequence} is a sequence of rational boxes\n\[\n  \cbox{a_n}{b_n}_{n\in\N}.\n\]\n\end{definition}\n\n\begin{definition}[complex number]\label{def:complex-raw}\n\lean{ComputableAnalysis.ComplexRaw}\n\lean{ComputableAnalysis.ComplexRaw.realPart}\n\lean{ComputableAnalysis.ComplexRaw.imagPart}\n\uses{def:box-sequence,def:raw-equiv}\nA \emph{complex number} is a nested box sequence\n\[\n  z=\cbox{a_n}{b_n}_{n\in\N}\n\]\nsatisfying\n\[\n  n\le m\Rightarrow a_n\le a_m\le b_m\le b_n,\n\]\nand whose real and imaginary widths both shrink to \(0\).  Equivalently, its\ncoordinate interval sequences are real numbers.  Two complex numbers are\nequivalent when their boxes overlap at every stage, i.e. coordinatewise\nequivalence of the real and imaginary parts.\n\end{definition}\n\n\begin{definition}[Complex arithmetic]\label{def:complex-arithmetic}\n\lean{ComputableAnalysis.ComplexRaw.add}\n\lean{ComputableAnalysis.ComplexRaw.mul}\n\lean{ComputableAnalysis.QBox.mulRealInterval_contains}\n\lean{ComputableAnalysis.QBox.mul_contains}\n\lean{ComputableAnalysis.QBox.mulRealInterval_width_le_of_abs_bounded}\n\lean{ComputableAnalysis.QBox.mul_width_height_le_of_coordinateBounded}\n\lean{ComputableAnalysis.QBox.mul_overlaps_of_overlaps}\n\lean{ComputableAnalysis.ComplexRaw.mul_valid}\n\lean{ComputableAnalysis.ComplexRaw.mul_equiv}\n\lean{ComputableAnalysis.ComplexRaw.qcomplexLeftMul_equiv_mul_ofQComplex}\n\lean{ComputableAnalysis.ComplexRaw.mulI}\n\lean{ComputableAnalysis.ComplexRaw.mulI_valid}\n\lean{ComputableAnalysis.ComplexRaw.imaginaryAxis}\n\lean{ComputableAnalysis.ComplexRaw.imaginaryAxis_valid}\n\uses{def:complex-raw,def:raw-real-arithmetic}\nFor complex numbers as box sequences\n\[\n  z=\cbox{a_n}{b_n}_{n\in\N},\qquad\n  w=\cbox{c_n}{d_n}_{n\in\N},\n\]\naddition is stagewise:\n\[\n  z+w:=\cbox{a_n+c_n}{b_n+d_n}_{n\in\N}.\n\]\nNegation reverses the box:\n\[\n  -z:=\cbox{-b_n}{-a_n}_{n\in\N}.\n\]\nFor multiplication, write the same box sequences in coordinates as\n\[\n  z=x+iy,\qquad w=u+iv,\n\]\nwhere \(x,y,u,v\) are real numbers.  Then define\n\[\n  zw:=(xu-yv)+i(xv+yu).\n\]\nAt the level of rational boxes, multiplication is implemented by applying\nreal interval arithmetic to these four real products and taking bounding\nintervals.  The four-corner interval contains every enclosed rational product,\nis ordered, and refines when both input intervals refine; the corresponding\ncomplex box satisfies the same three facts.  This already gives a certified\nraw product: every later input box lies in its stage-zero box, whose explicit\nrational coordinate radius yields a product-width modulus.  The resulting\nproduct respects raw overlap equivalence, witnessed at each stage by rational\npoints in the two finite box intersections.\nFor an exact rational complex scalar, the certified affine action\n\lean{ComputableAnalysis.ComplexRaw.qcomplexLeftMul}\n\lean{ComputableAnalysis.ComplexRaw.qcomplexLeftMul_valid}\n\lean{ComputableAnalysis.ComplexRaw.qcomplexLeftMul_equiv}\nis already available as a lower-cost exact implementation, with the two real\nscalar actions and the direct imaginary-unit rotation as its finite\nimplementation.  The already certified special case\n\(iz=-\operatorname{im}(z)+i\operatorname{re}(z)\)\nis a coordinate rotation: it reverses one pair of endpoints and exchanges the\ncoordinates.  Hence a certified real \(x\) already gives a certified complex\nhandle \(ix\), and \(i\pi/2\) is literally its exact \(i/2\)-scalar action.\nThe affine scalar action is proved equivalent to ordinary multiplication by\nits exact rational complex box, so either evaluator can be used in a\nrepresentation-safe calculation.\n\end{definition}\n\n\begin{definition}[Complex inverse]\label{def:complex-inverse}\n\lean{ComputableAnalysis.ComplexRaw.ApartZero}\n\lean{ComputableAnalysis.ComplexRaw.HasComputableInv}\n\uses{def:complex-arithmetic,def:raw-real-arithmetic,def:raw-real-sign}\nA complex number \(z=x+iy\) is \emph{nonzero} if\n\[\n  x^2+y^2\n\]\nis nonzero as a real number.  For nonzero \(z\), define\n\[\n  z^{-1}\n  :=\n  \frac{x}{x^2+y^2}\n  -\n  i\,\frac{y}{x^2+y^2}.\n\]\n\end{definition}\n\n\begin{definition}[Certified complex handles]\label{def:complex-quotient}\n\lean{ComputableAnalysis.Complex}\n\uses{def:complex-raw}\nThe current Lean-facing complex type stores a preferred certified complex-box\nalgorithm and a finite list of certified equivalent alternatives.  As for\n\code{Real}, this is a computation-oriented representation layer rather than\na quotient construction.\n\end{definition}\n\n\begin{proposition}[Complex raw arithmetic is representation-aware]\label{thm:complex-field}\n\uses{def:complex-quotient,def:complex-arithmetic,def:complex-inverse}\nAddition, negation, rational scaling, real-axis embedding, and the\nimaginary-unit rotation carry validity lemmas.  General complex product now\ncarries both its validity and overlap-congruence theorems; complex inverse\nremains an explicit certification target.  The project does not claim a\nquotient field structure for \code{Complex}; scientific constructions use\nonly certified raw operations and their accompanying overlap proofs.\n\end{proposition}\n\n\begin{proposition}[Available rational and real embeddings]\label{thm:rational-complex-field-square}\n\lean{ComputableAnalysis.ComplexRaw.ofQComplex}\n\lean{ComputableAnalysis.Complex.ofQComplex}\n\lean{ComputableAnalysis.Complex.ofReal}\n\uses{def:qcomplex,def:complex-quotient,thm:raw-reals-field,thm:complex-field}\nLean provides constant rational-complex boxes and embeds certified reals on the\nreal axis.  At the representation level the intended diagram is\n\[\n\begin{array}{ccc}\n\Q & \longrightarrow & \Q(i)\\\n\downarrow & & \downarrow\\\n\R & \longrightarrow & \C\n\end{array}\n\]\nbut no field-homomorphism or quotient claim is used by the present calculus.\n\end{proposition}\n\n\section{Functions and domains}\n\n\begin{definition}[Complex-valued function representation]\label{def:function-raw}\n\lean{ComputableAnalysis.FunctionRaw}\n\uses{def:qcomplex,def:complex-raw}\nA \emph{complex-valued function representation} consists of:\n\begin{enumerate}\n  \item a rational domain predicate \(D_f\subseteq\Q(i)\), and\n  \item a finite stage algorithm which, given \(z\in D_f\) and \(n\in\N\),\n  returns a rational complex box \(B_f(z,n)\).\n\end{enumerate}\nThe pointwise validity requirement is:\nfor every fixed rational input \(z\in D_f\), the sequence\n\[\n  n\longmapsto B_f(z,n)\n\]\nis a complex number in the sense of \cref{def:complex-raw}.  At the\ncomputational layer, an input is supplied by a rational name and evaluation is\na stage-indexed box algorithm.  The domain may be all rational names in a\nregion, or only names in a branch domain, and every evaluation carries the\ndomain proof.\n\end{definition}\n\n\begin{definition}[Real-input and real-valued functions]\label{def:real-fun-raw}\n\lean{ComputableAnalysis.RealFunRaw}\n\lean{ComputableAnalysis.RealFunRaw.add_valid}\n\lean{ComputableAnalysis.PartialRealFunRaw}\n\uses{def:function-raw,thm:rational-complex-field-square}\nA real-input real-valued function is the real-axis specialization of\n\cref{def:function-raw}.  It consists of a rational domain predicate\n\(D_f\subseteq\Q\) and a stage algorithm\n\[\n  (x,n)\longmapsto [a_f(x,n),b_f(x,n)],\n  \qquad x\in D_f,\quad n\in\N,\n\]\nsuch that for every fixed rational \(x\in D_f\), the sequence\n\[\n  n\longmapsto [a_f(x,n),b_f(x,n)]\n\]\nis a real number in the sense of \cref{def:raw-real}.  In applications we often\nalso require \(D_f\) to contain at least one nontrivial rational interval; this\nis recorded by the interval wrappers below rather than by changing the basic\nfunction definition.\n\nLean has two presentations of this same idea.  \code{RealFunRaw} keeps the\n\code{compute} field total on rational inputs and stores a domain predicate\nsaying where it is valid.  \code{PartialRealFunRaw} makes the domain proof an\nargument to \code{compute}.  In both cases, validity means exactly the\npointwise condition above: for each fixed rational input in the domain, the\nstage sequence is a raw real.\n\end{definition}\n\n\begin{definition}[Rational segment]\label{def:rational-segment}\n\uses{def:qcomplex}\nA \emph{rational segment} in the complex plane is determined by two distinct\nrational complex points \(p,q\in\Q(i)\).  Its rational points are\n\[\n  (1-t)p+tq,\qquad t\in\Q,\quad 0\le t\le 1.\n\]\nThe segment is real when \(p\) and \(q\) lie on the rational real axis.\n\end{definition}\n\n\begin{definition}[Agreement on an overlap]\label{def:function-agrees-on-overlap}\n\uses{def:function-raw,def:rational-segment,def:complex-raw}\nLet \(f\) and \(g\) be complex-valued function representations.  An\n\emph{agreement-on-overlap certificate} consists of a nontrivial rational\nsegment \(S\) such that every rational point of \(S\) lies in both domains, and\nfor every rational point \(q\in S\), the complex numbers \(f(q)\) and \(g(q)\)\nare equivalent.\n\nThis is a local agreement statement with an explicit witness.  For arbitrary\nfunctions it is not promoted to a global equivalence relation; transitivity\nrequires additional analytic structure.\n\end{definition}\n\n\begin{definition}[Function on an interval]\label{def:function-on-interval}\n\lean{ComputableAnalysis.FunctionOnInterval}\n\uses{def:qinterval,def:real-fun-raw,def:rational-segment}\nA function on a rational interval \([a,b]\) is the real-axis special case of\nthe preceding setup: the interval is the rational segment from \(a+i0\) to\n\(b+i0\), the function is defined at every rational point of that segment, and\nthe pointwise outputs are real numbers.\n\end{definition}\n\n\begin{definition}[Interval regularity (rational continuity)]\label{def:interval-regular}\n\lean{ComputableAnalysis.IntervalRegularOn}\n\lean{ComputableAnalysis.EpsilonDeltaContinuousOn}\n\uses{def:function-on-interval}\nLet \(F\) be a real-valued function on \([a,b]\).  An interval-regularity\ncertificate assigns to each rational subinterval\n\[\n  K=[r,s]\subseteq [a,b]\n\]\nand each stage \(n\) a rational output interval \(Y(K,n)\).  It supplies a\nmodulus \(M:\N\to\N\), positive at every stage, such that whenever\n\[\n  s-r\le \frac{1}{M(n)},\n\]\nand \(Y(K,n)=[u,v]\), we have\n\[\n  0\le v-u\le \frac{1}{n+1},\n\]\nand for every rational point \(x\in K\), the point-value interval\n\(F(x,n)\) is enclosed by \(Y(K,n)\).  Thus this is genuinely\ninterval-valued continuity: it controls both the separation of values and\nthe widths of their rational enclosures.  In particular, this is the\nproject's epsilon--delta notion of continuity, stated only over rational\ninputs and rational tolerances: given \(\varepsilon>0\), it computes a\n\(\delta>0\) and a common evaluation stage at which inputs within\n\(\delta\) have \(\varepsilon\)-near output boxes.  No topology or completed\nreal line is part of the definition.\n\end{definition}\n\n\begin{theorem}[Constructive monotone inverse-function theorem]\n\label{thm:constructive-inverse-function}\n\lean{ComputableAnalysis.EffectiveInverseSeparation}\n\lean{ComputableAnalysis.InvertibleFunctionOnInterval}\n\lean{ComputableAnalysis.InRangeRaw}\n\lean{ComputableAnalysis.InverseRaw}\n\lean{ComputableAnalysis.HasInverse}\n\lean{ComputableAnalysis.HasBisectionSearch}\n\lean{ComputableAnalysis.inverse_function_from_bisection_search}\n\lean{ComputableAnalysis.monotoneBisectionIterate_preserves_target_bracket}\n\lean{ComputableAnalysis.monotoneBisectionIterate_certificate}\n\lean{ComputableAnalysis.monotoneTargetBisectionIterate_add}\n\lean{ComputableAnalysis.monotoneTargetBisectionIterate_later_subinterval}\n\lean{ComputableAnalysis.monotoneTargetBisectionIterate_tolerance_certificate}\n\lean{ComputableAnalysis.monotoneBisectionIterate_width}\n\lean{ComputableAnalysis.monotoneBisectionIterate_width_le_of_power_budget}\n\lean{ComputableAnalysis.affineHalf}\n\lean{ComputableAnalysis.affineHalfInitial}\n\lean{ComputableAnalysis.affineHalf_bisection_stage3}\n\lean{ComputableAnalysis.affineHalf_bisection_stage3_bracket}\n\lean{ComputableAnalysis.affineHalf_bisection_stage3_width}\n\lean{ComputableAnalysis.squareTarget}\n\lean{ComputableAnalysis.squareTargetInitial}\n\lean{ComputableAnalysis.squareTarget_bisection_stage4}\n\lean{ComputableAnalysis.squareTarget_bisection_stage4_bracket}\n\lean{ComputableAnalysis.squareTarget_bisection_stage4_width}\n\lean{ComputableAnalysis.cubeTarget}\n\lean{ComputableAnalysis.cubeTargetInitial}\n\lean{ComputableAnalysis.cubeTarget_bisection_stage4}\n\lean{ComputableAnalysis.cubeTarget_bisection_stage4_bracket}\n\lean{ComputableAnalysis.cubeTarget_bisection_stage4_width}\n\lean{ComputableAnalysis.cubeTarget_bisection_stage8}\n\lean{ComputableAnalysis.cubeTarget_bisection_stage8_bracket}\n\lean{ComputableAnalysis.cubeTarget_bisection_stage8_width}\n\lean{ComputableAnalysis.cubeTarget_bisection_stage16}\n\lean{ComputableAnalysis.cubeTarget_bisection_stage16_bracket}\n\lean{ComputableAnalysis.cubeTarget_bisection_stage16_width}\n\lean{ComputableAnalysis.cubeTarget_bisection_stage24}\n\lean{ComputableAnalysis.cubeTarget_bisection_stage24_bracket}\n\lean{ComputableAnalysis.cubeTarget_bisection_stage24_width}\n\lean{ComputableAnalysis.fourthTarget}\n\lean{ComputableAnalysis.fourthTargetInitial}\n\lean{ComputableAnalysis.fourthTarget_bisection_stage8}\n\lean{ComputableAnalysis.fourthTarget_bisection_stage8_bracket}\n\lean{ComputableAnalysis.fourthTarget_bisection_stage8_width}\n\lean{ComputableAnalysis.fourthTarget_bisection_stage16}\n\lean{ComputableAnalysis.fourthTarget_bisection_stage16_bracket}\n\lean{ComputableAnalysis.fourthTarget_bisection_stage16_width}\n\lean{ComputableAnalysis.fourthTarget_bisection_stage24}\n\lean{ComputableAnalysis.fourthTarget_bisection_stage24_bracket}\n\lean{ComputableAnalysis.fourthTarget_bisection_stage24_width}\nThe cubic target (x^3=2) now has a stage-eight rational trace\n([161/128,323/256]), with width (1/256), and a stage-sixteen trace\n([41285/32768,82571/65536]) with width (1/65536); both have certified\nendpoint comparisons.  These are finite inverse-search witnesses, not an\nattained cube root.\nThe fourth-power target (x^4=2) now has corresponding stage-eight and\nstage-sixteen traces, with widths (1/256) and (1/65536).  This extends the\nnonlinear inverse-search ladder without asserting an attained fourth root.\nBoth traces now also reach stage 24, with width (1/16777216), and retain\ntheir exact endpoint bracket proofs.\n\lean{ComputableAnalysis.induction_arithmeticSum_eq}\n\lean{ComputableAnalysis.induction_arithmeticSum_stage5}\n\lean{ComputableAnalysis.induction_arithmeticSum_stage10}\n\lean{ComputableAnalysis.induction_arithmeticSum_stage20}\n\lean{ComputableAnalysis.induction_arithmeticSum_stage40}\n\lean{ComputableAnalysis.induction_arithmeticSum_stage80}\n\lean{ComputableAnalysis.induction_arithmeticSum_stage160}\n\lean{ComputableAnalysis.subsetCount_stage8}\n\lean{ComputableAnalysis.subsetCount_stage8_pow}\n\lean{ComputableAnalysis.subsetCount_stage8_certificate}\n\lean{ComputableAnalysis.subsetCount_stage10}\n\lean{ComputableAnalysis.subsetCount_stage10_pow}\n\lean{ComputableAnalysis.subsetCount_stage10_certificate}\n\lean{ComputableAnalysis.subsetCount_stage12}\n\lean{ComputableAnalysis.subsetCount_stage12_pow}\n\lean{ComputableAnalysis.subsetCount_stage12_certificate}\n\lean{ComputableAnalysis.subsetCount_stage16}\n\lean{ComputableAnalysis.subsetCount_stage16_pow}\n\lean{ComputableAnalysis.subsetCount_stage16_certificate}\n\lean{ComputableAnalysis.subsetCount_stage20}\n\lean{ComputableAnalysis.subsetCount_stage20_pow}\n\lean{ComputableAnalysis.subsetCount_stage20_certificate}\n\lean{ComputableAnalysis.subsetCount_stage32}\n\lean{ComputableAnalysis.subsetCount_stage32_pow}\n\lean{ComputableAnalysis.subsetCount_stage32_certificate}\n\lean{ComputableAnalysis.subsetCount_stage64}\n\lean{ComputableAnalysis.subsetCount_stage64_pow}\n\lean{ComputableAnalysis.subsetCount_stage64_certificate}\n\uses{def:function-on-interval,def:interval-regular}\nFix an interval-regular monotone branch \(I\) on a rational source interval.\nIn addition to monotonicity, it carries an effective inverse-separation\ncertificate: at every requested scale, inputs separated by a stated positive\nrational amount have disjointly ordered output boxes, in the one orientation\n(nondecreasing or nonincreasing) chosen for the branch.  That orientation is\nrequired to agree with the monotonicity witness.  For every valid raw target\nthat is explicitly enclosed by the endpoint-value boxes of \(I\), suppose we\ngive a certified finite bisection search.  Lean then proves the branch-local\nassembly theorem\n\[\n  \code{HasBisectionSearch}(I)\n\Longrightarrow \code{HasInverse}(I).\n\]\nThis branch-local bisection assembly is the constructive core of benchmark\nitem 79 (intermediate-value search); it is not a blanket classical IVT over an\nunrepresented completed real line.\nThus, once a particular bisection/search is supplied for each computable\ntarget with a certified range witness, it gives a raw interval algorithm for\nthe preimage.  Such a target is itself a valid raw real, and for every target\nstage it carries an explicit precision at which its output box lies between the\ncorrectly oriented endpoint-value boxes of \(I\).  The theorem does not obtain\nthat finite search by a choice principle.\nThus the inverse is another interval algorithm, not a choice of a point from\na completed continuum.  Effective separation is essential: monotonicity alone\ndoes not give a usable finite inverse search for a function that may be\narbitrarily flat.\n\end{theorem}\n\n\begin{example}[Algebraic branches]\n\label{ex:algebraic-inverse-branches}\n\lean{ComputableAnalysis.squareOnUnit_invertible}\n\lean{ComputableAnalysis.squareOnUnitRationalTarget}\n\lean{ComputableAnalysis.sqrtOnUnitBisectionSearch}\n\lean{ComputableAnalysis.FirstYearCalculus.sqrtOnUnitBisectionSearch_stage_certificate}\n\lean{ComputableAnalysis.FirstYearCalculus.sqrtOnUnitBisectionSearch_nested_stage_certificate}\n\lean{ComputableAnalysis.sqrtOnUnitRepresentedTargetSearch}\n\lean{ComputableAnalysis.sqrtOnUnitRepresentedTargetSearch_stage_certificate}\n\lean{ComputableAnalysis.sqrtRaw_spec}\n\lean{ComputableAnalysis.sqrtOnUnit}\n\lean{ComputableAnalysis.sqrtOnUnit_intervalRegular}\n\lean{ComputableAnalysis.sqrtOnUnit_epsilonDeltaContinuous}\n\uses{thm:constructive-inverse-function}\nThe first concrete forward package is squaring on \([0,1]\): its evaluator is\nthe exact rational box \([r^2,r^2]\), it has a direct rational interval\nmodulus, is nondecreasing, and has a nondecreasing effective-separation\ncertificate.  Lean's \code{squareOnUnit\_invertible} packages these data.\nThe existing nonnegative square-root bisection intervals\nisolate the solution of \(w^2=q\), are valid raw reals, and satisfy the\nalgebraic specification.  For every exact rational target \(q\in[0,1]\),\nLean packages this exact-target bisection as\n\code{sqrtOnUnitBisectionSearch}: it remains inside the source interval and\nits squared interval overlaps the target box.  The exact target box is also\nformally enclosed by the square map's endpoint boxes at every stage, so it is\na genuine certified range input to the inverse interface.  Extending that\nfinite search uniformly to every represented target on the unit range is the\nnext inverse-function step; no inverse is asserted merely from monotonicity.\nSeparately, Lean now packages the scheduled bisection evaluator itself as the\ninterval-regular function \code{sqrtOnUnit}.  For an input interval of width\nat most \(1/(16(n+1)^2)\), its endpoint boxes, enlarged by certified bisection\nerrors, enclose every point box and have width at most \(1/(n+1)\).  Thus\n\(\sqrt{\phantom{x}}\) on \([0,1]\) already satisfies the literal rational\n\(\varepsilon\)--\(\delta\) continuity predicate, without a topology import.\nGeneral algebraic functions will use isolating boxes, separation, and\nbisection or subdivision to construct selected branches.\nThe companion finite cube-root trace applies the same target search to\n(x^3=2) on ([1,2]): stage four returns ([5/4,21/16]), preserves the\ntarget bracket, and has width (1/16).  This supplies a second nonlinear\ninverse trace without introducing a completed cube-root value.\n\end{example}\n\n\begin{remark}[Continuous extension of represented functions]\n\label{rem:continuous-extension-of-function-representations}\nRational names are the finite interface of the representation, not the\nintended mathematical domain.  A continuous representation with an effective\nmodulus can later be extended from rational names to all computable real\ninputs; the complex analogue extends it to a specified open computable domain.\nOnly functions equipped with the necessary continuity and domain certificates\nadmit this extension.  The present chapters use the rational-name interface\nuntil that extension theorem is constructed.\n\end{remark}\n\n\n\begin{remark}[How the later elementary functions fit]\n\label{rem:elementary-functions-are-function-representations}\n\uses{def:real-fun-raw,def:function-on-interval}\nThe elementary functions constructed later all use this same shape.\nPower-series arctangent has domain \([-1,1]\) and sends \((x,n)\) to an\nalternating-series interval.  The next chapter is planned to obtain the first\nsine and cosine branch by applying the preceding inverse theorem to geometric\narctangent, recovering the bounded tangent slope, and evaluating the rational\ncircle chart at that slope.  Its two coordinate projections are cosine and\nsine.  Exponential is\nrepresented by power-series, compound-interest, and inverse-log-integral\nstage algorithms.  Logarithm is a \code{FunctionOnInterval} on positive\nbranches, defined by the integral of \(1/x\).  In every case, fixing a\nrational name in the stated domain leaves an interval sequence in the stage\nvariable, and the validity proof is precisely the proof that this sequence is\na raw real.\n\end{remark}\n\n\begin{theorem}[Interval regularity implies rational continuity]\n\label{thm:interval-regular-implies-epsilon-delta}\n\lean{ComputableAnalysis.IntervalRegularOn.epsilonDeltaContinuous}\n\uses{def:interval-regular}\n\leanok\nEvery interval-regular function satisfies the literal rational\n\(\varepsilon\)-\(\delta\) predicate used elsewhere in the project.  For a\npositive rational \(\varepsilon\), Lean takes the finite stage\n\(n=\operatorname{den}(\varepsilon)+1\), so \(1/n\le\varepsilon\), and\nuses \(\delta=1/M(n)\).  The smallest rational interval containing two\ninputs within \(\delta\) has an image enclosure of width at most\n\(1/n\).  Since both point-value boxes are contained in that one enclosure,\nthey are \code{NearAt} within \(\varepsilon\), including their individual\nwidth bounds.  This is a direct finite rational proof, with no topology or\nreal-number completeness principle.\n\end{theorem}\n\n\begin{remark}[Holomorphic branch equivalence]\label{rem:holomorphic-branch-equivalence}\n\uses{def:function-agrees-on-overlap}\nFor later complex analysis, holomorphic function representations will carry\nstronger domain data.  On that layer, overlap should be witnessed by a\nnonempty open region, such as a rational disk contained in the intersection of\nthe domains.  The identity principle can then turn local agreement on such an\noverlap into a transitive branch-equivalence relation.\n\end{remark}\n\n\begin{remark}[Completeness is replaced by moduli]\label{rem:completeness-replaced-by-moduli}\n\uses{def:interval-regular}\nClassical compactness, uniform continuity, and existence of suprema are not\nused as black boxes.  The project records the finite data that those theorems\nusually hide: interval images, rates, nesting, and enclosure proofs.\n\end{remark}\n