\chapter{The FTC Bridge: Effective Calculus}\label{ch:effective-calculus}

This chapter is the proof layer for the FTC contract stated in
Chapter~\ref{ch:integrals}.  It supplies the finite certificates that a
particular evaluator must provide; it is not a second integral theory and
does not define a competing integral.  In the printed book it sits directly
between Integrals and Power Series.  The resulting dependency is:

\begin{verbatim}
circle/arctan -> integral + FTC contract -> FTC bridge: how a function earns it
                                           -> power series -> exp/log -> ODEs
\end{verbatim}

Special-function evaluators should expose a stable computation
\((x,n)\mapsto [a,b]\). Domain proofs remain available for safety, but they
are not part of the algorithm. The proof-relevant function interface adapts
such evaluators without changing the interval semantics. Regularity is stated
directly for the stable evaluator and transported once to the public API.
The exact rational constant, affine evaluator, and the nonlinear square and
cube evaluators on \([0,1]\) are checked instances of this interface; a special
function adds only its finite range and overlap lemmas.
The square and cube instances also carry finite dyadic integral certificates,
so the pointwise modulus is already connected to an actual computable area.

The same foundation also has a genuinely nonconstant Lipschitz--Darboux
route.  For the identity function, midpoint refinement gives nested rational
boxes of width \(2/2^n\); this is the model for integrating functions whose
values are not captured by one exact rectangle.  It is deliberately kept
separate from the affine FTC evaluator so that later special functions can
reuse the rectangle construction without first acquiring a symbolic
primitive.  The complete example identifies the resulting raw integral:
\(\int_0^1 x\,dx=1/2\).

\lean{ComputableAnalysis.IntegralIdentities.affineIdentity_lipschitz_on_unit}
\lean{ComputableAnalysis.IntegralIdentities.affineIdentityLipschitzConstruction}
\lean{ComputableAnalysis.IntegralIdentities.affineIdentityLipschitzConstruction_valid}
\lean{ComputableAnalysis.IntegralIdentities.affineIdentity_uniformLeftEndpointSum_eq}
\lean{ComputableAnalysis.IntegralIdentities.affineIdentity_uniformRightEndpointSum_eq}
\lean{ComputableAnalysis.IntegralIdentities.affineIdentityLipschitz_integral_equiv_half}
\lean{ComputableAnalysis.IntegralIdentities.LipschitzDyadic.lipschitzCells_refine}
\lean{ComputableAnalysis.IntegralIdentities.LipschitzDyadic.compute_nested}
\lean{ComputableAnalysis.IntegralIdentities.LipschitzDyadic.compute_width}
\lean{ComputableAnalysis.IntegralIdentities.LipschitzDyadic.compute_widthsShrink}
\leanok

The first complete end-to-end FTC witness is affine: its finite-difference
derivative is exactly constant, and a single rectangle computes the integral
exactly.  This is the base case for the later polynomial and special-function
certificates.

\lean{ComputableAnalysis.riemannLeftExact_constant_one}
\lean{ComputableAnalysis.riemannLeftExact_affine_closed}
\lean{ComputableAnalysis.riemannLeftExact_affine_error_of_pos}
\lean{ComputableAnalysis.ExactFunction.affineFTCExact}
\lean{ComputableAnalysis.ExactFunction.ftcErrorExact_square_doubleId_of_pos}
\lean{ComputableAnalysis.ExactFunction.ftcErrorExact_cube_threeSquare_unit_of_pos}
\lean{ComputableAnalysis.riemannLeftExact_doubleId_of_pos}
\lean{ComputableAnalysis.riemannLeftExact_threeSquare_unit_of_pos}
\lean{ComputableAnalysis.riemannLeftExact_affine_substitution}
\lean{ComputableAnalysis.ExactFunction.squareFTCExactUnit}
\lean{ComputableAnalysis.Integral.cubeEffectiveFTCData}
\lean{ComputableAnalysis.Integral.cubeEffectiveFTC_equiv_endpoint}
\lean{ComputableAnalysis.Integral.cubicIntegralEffectiveFTCData}
\lean{ComputableAnalysis.Integral.cubicIntegralEffectiveFTC_equiv_endpoint}
\lean{ComputableAnalysis.Integral.cubicIntegralEffectiveFTC_equiv_one_fourth}
\leanok

The polynomial ladder is now visible at the public monotone-integral level.
On $[0,1]$, factorization proves that $x^2$, $x^3$, and $x^4$ are
nondecreasing; their finite rectangle certificates identify the integrals
with $1/3$, $1/4$, and $1/5$.  These are not appeals to an all-polynomials
integration theorem: each evaluator carries its own rational construction and
overlap proof.  They provide the first reusable test family for the effective
FTC before special functions enter.

\lean{ComputableAnalysis.Integral.exactRat_square_nondecreasing}
\lean{ComputableAnalysis.Integral.exactRat_square_monotone_integral_valid}
\lean{ComputableAnalysis.Integral.exactRat_square_monotone_integral_eq_one_third}
\lean{ComputableAnalysis.Integral.exactRat_cube_nondecreasing}
\lean{ComputableAnalysis.Integral.exactRat_cube_monotone_integral_valid}
\lean{ComputableAnalysis.Integral.exactRat_cube_monotone_integral_eq_one_fourth}
\lean{ComputableAnalysis.Integral.exactRat_quartic_nondecreasing}
\lean{ComputableAnalysis.Integral.exactRat_quartic_monotone_integral_valid}
\lean{ComputableAnalysis.Integral.exactRat_quartic_monotone_integral_eq_one_fifth}
\leanok

The square now also closes the full derivative-bound route.  The primitive
$x^3/3$ and derivative $x^2$ are represented by rational interval evaluators;
the effective FTC identifies the bounded rectangle computation with the
endpoint difference, and the endpoint computation is reduced to $1/3$ by
finite rational arithmetic.  This is the model theorem for the later
special-function instances: the FTC and the value identification are two
separate overlap steps.

The cubic uses the same contract with primitive $x^4/4$ and closes at
$1/4$; the proof changes only the finite power-range bound.

The same finite schedule is already checked for the quartic and fifth
monomial targets, closing at $1/5$ and $1/6$.  Together these examples give a
small regression family for the derivative-bound FTC before the project moves
to exponential and trigonometric evaluators.

\lean{ComputableAnalysis.Integral.squareIntegralEffectiveFTC_endpoint_equiv_one_third}
\lean{ComputableAnalysis.Integral.squareIntegralEffectiveFTC_integral_equiv_one_third}
\lean{ComputableAnalysis.Integral.cubicIntegralEffectiveFTC_integral_equiv_one_fourth}
\lean{ComputableAnalysis.Integral.quarticIntegralEffectiveFTC_equiv_one_fifth}
\lean{ComputableAnalysis.Integral.fifthIntegralEffectiveFTC_equiv_one_sixth}
\leanok

\section{Continuity as a finite certificate}

The public interval-function layer supports same-chart addition.  This is the
first algebraic closure needed before proving derivative and FTC closure
rules: evaluator boxes add stage by stage, while every analytic property is
still supplied by its own finite certificate.

\lean{ComputableAnalysis.FunctionOnInterval.add}
\lean{ComputableAnalysis.FunctionOnInterval.add_compute}
\leanok

Continuity is not a predicate on an ambient completed function space.  A
function exposes either a pointwise modulus or an interval image algorithm:

\begin{verbatim}
requested output width -> input-width budget -> rational image box
                                           -> contains every point value
\end{verbatim}

The interval form is stronger for integration because it gives one box for an
entire cell.  It implies the rational epsilon--delta form constructively:

For addition, the finite-difference algebra is explicit:

\begin{verbatim}
(F+G)(x+h)-(F+G)(x)
  = (F(x+h)-F(x)) + (G(x+h)-G(x))
\end{verbatim}

Interval quotients obey the same identity.  If two quotient boxes are near
their derivative boxes at an internal stage, their sum is near the summed
derivative box at the requested stage after paying the doubled error budget.
This is the constructive replacement for silently invoking a derivative
closure theorem over completed real-valued functions.

Scalar multiplication is sign-complete at the same level.  A rational factor
uses an explicit budget \(|r|\,\varepsilon_{\mathrm{inner}}\leq
\varepsilon_{\mathrm{out}}\); negative factors are reduced to a nonnegative
factor followed by interval negation.  Thus finite linear combinations do not
depend on an implicit ordered-real scalar operation.

\lean{ComputableAnalysis.QInterval.differenceQuotient_addInterval}
\lean{ComputableAnalysis.intervalNearAtPrecision_addInterval}
\lean{ComputableAnalysis.HasDerivativeOnInterval.addOfCommonSchedule}
\lean{ComputableAnalysis.intervalNearAtPrecision_scaleRat_of_nonneg}
\lean{ComputableAnalysis.HasDerivativeOnInterval.scaleRat_of_nonneg}
\lean{ComputableAnalysis.HasDerivativeOnInterval.scaleRat}
\lean{ComputableAnalysis.HasDerivativeOnInterval.linearCombinationOfCommonSchedule}
\lean{ComputableAnalysis.FunctionOnInterval.neg}
\lean{ComputableAnalysis.HasDerivativeOnInterval.neg}
\lean{ComputableAnalysis.FunctionOnInterval.sub}
\lean{ComputableAnalysis.HasDerivativeOnInterval.subOfCommonSchedule}
\leanok

Affine reparametrization has the corresponding finite chain rule.  For a
nonzero slope the inner step is transported exactly; the zero-slope case is
also included because a constant affine map has zero finite difference
quotient.  This is the rational core used by substitutions and normalized
angle charts:

\begin{verbatim}
f(m(x+h)+c)-f(mx+c)     m * (f(y+m h)-f(y))
--------------------  =  --------------------
          h                         m h
\end{verbatim}

\lean{ComputableAnalysis.ExactFunction.differenceQuotient_affine_comp}
\lean{ComputableAnalysis.ExactFunction.differenceQuotient_affine_comp_of_step}
\lean{ComputableAnalysis.ExactFunction.secant_bracket_affine_endpoint_transport}
\leanok

The same finite mechanism now closes multiplication when rational majorants
are supplied for both factors and both proposed derivatives.  The certificate
contains the explicit corner term coming from the product finite difference;
the wrapper below exposes it as an interval derivative certificate for the
exact rational product chart.  Thus the product rule is available here as a
bounded computational theorem, while the unrestricted textbook version is
not smuggled in through completeness of the reals.

\lean{ComputableAnalysis.FinitePolynomial.SecantDerivativeBound.mul}
\lean{ComputableAnalysis.FinitePolynomial.SecantDerivativeBound.mulToHasDerivativeOnInterval}
\leanok

The local hand-off from differentiation to integration is also finite.  On a
positive rational cell whose width satisfies the derivative step budget, the
secant certificate scales back to an endpoint-difference enclosure.  The
derivative box is expanded by twice the requested precision; this is the
explicit error budget later consumed by a partition-level FTC certificate.

\lean{ComputableAnalysis.HasDerivativeOnInterval.endpointDifference_contains_of_pos}
\leanok

\lean{ComputableAnalysis.EffectiveModulusFor}
\lean{ComputableAnalysis.IntervalRegularOn}
\lean{ComputableAnalysis.IntervalRegularOn.epsilonDeltaContinuous}
\lean{ComputableAnalysis.ScheduledIntervalRegularOn}
\lean{ComputableAnalysis.ScheduledIntervalRegularOn.toIntervalRegularOn_of_requested_stage}
\lean{ComputableAnalysis.ScheduledIntervalRegularOn.toIntervalRegularOn_of_identity_schedule}
\lean{ComputableAnalysis.ScheduledIntervalRegularOn.epsilonDeltaContinuous}
\lean{ComputableAnalysis.ContinuousFunctionOnInterval}
\lean{ComputableAnalysis.Integral.unitConstantMonotoneDarbouxSchedule}
\lean{ComputableAnalysis.Integral.unitConstantMonotoneDarbouxSchedule_integral_eq_one}
\leanok

This implication uses only rational endpoint hulls, interval containment, and
the requested denominator.  The reverse direction is not automatic: an
epsilon--delta modulus must still be upgraded to a cell-image evaluator before
it can drive a Darboux integral.

Adaptive evaluators use `ScheduledIntervalRegularOn`: their cell image and
point value are evaluated at one explicit stage, which may differ from the
native stage index.  A separate validity/equivalence proof is still required
before that representation is installed in the abstract-function chain.  The
explicit adapter above requires the missing requested-stage containment
certificate; monotonicity of raw boxes alone is insufficient because later
boxes may be wider.

For the ordinary identity schedule, the
\texttt{toIntervalRegularOn\_of\_identity\_schedule} adapter discharges this bookkeeping
from the scheduled contract itself.  Adaptive schedules still require an
explicit transport certificate.

Representations form a local chain: reflexive, symmetric, and transitive
equivalence edges compose different rational stage schedules without changing
the represented function.

\lean{ComputableAnalysis.FunctionOnInterval.equivalent_refl}
\lean{ComputableAnalysis.FunctionOnInterval.equivalent_symm}
\lean{ComputableAnalysis.FunctionOnInterval.equivalent_trans}
\leanok

The registry is maintained as a spanning tree, not as a table of every
pairwise comparison.  To add a new evaluator, prove validity and one edge to
an existing representation:

\begin{verbatim}
new evaluator -> one existing evaluator -> semantic function
              (finite overlap edge)    (existing chain)
\end{verbatim}

The preferred evaluator can therefore be changed for efficiency without
rewriting theorems stated at the abstract function level.

For inverse functions, separation may depend on the actual rational gap
between two trial inputs.  The general contract therefore has two forms:

\begin{verbatim}
uniform precision schedule       -> EffectiveInverseSeparation
gap-dependent precision schedule -> GapAwareInverseSeparation
                                   -> bisection trace
\end{verbatim}

The second form is the computable one for flat or rapidly varying functions.
It does not ask for a limiting inverse value: each bracket carries its own
finite forward-image separation proof.

\lean{ComputableAnalysis.EffectiveInverseSeparation}
\lean{ComputableAnalysis.GapAwareInverseSeparation}
\lean{ComputableAnalysis.InvertibleFunctionOnInterval}
\lean{ComputableAnalysis.DyadicInverseTrace}
\lean{ComputableAnalysis.DyadicInverseTrace.toSearch}
\leanok

The proof-independent function layer exposes the same laws.  Its transitivity
hypothesis explicitly supplies the middle domain on the intersection of the
outer domains; this is the correct contract for partial special functions.

\lean{ComputableAnalysis.RealFunRaw.equivalentWith_refl}
\lean{ComputableAnalysis.RealFunRaw.equivalentWith_symm}
\lean{ComputableAnalysis.RealFunRaw.equivalentWith_trans}
\lean{ComputableAnalysis.RealFunRaw.equivalent_trans}
\leanok

The curvature route is exposed as a finite method rather than as a classical
second-derivative theorem.  A cell carries monotone derivative data; endpoint
secants then furnish the derivative bracket used by the FTC certificate.

\begin{verbatim}
monotone derivative on cell -> endpoint secants -> derivative interval
                            -> local endpoint bound -> effective FTC
\end{verbatim}

\lean{ComputableAnalysis.MonotoneDerivativeBoundMethod}
\lean{ComputableAnalysis.MonotoneDerivativeBoundMethod.toDerivativeBound}
\lean{ComputableAnalysis.CurvatureOnSubinterval}
\lean{ComputableAnalysis.DerivativeBoundFromCurvature.toDerivativeBound}
\lean{ComputableAnalysis.CurvatureFTCCertificate}
\lean{ComputableAnalysis.CurvatureFTCCertificate.toDerivativeBoundFTC}
\lean{ComputableAnalysis.curvatureFTC}
\lean{ComputableAnalysis.ConvexFTCCertificate.equiv_endpoint}
\lean{ComputableAnalysis.ConcaveFTCCertificate.equiv_endpoint}
\lean{ComputableAnalysis.constructionFor_of_selectedStageCandidateDerivativeFTC}
\lean{ComputableAnalysis.definiteIdentityFor_of_selectedStageCandidateDerivativeFTC}
\lean{ComputableAnalysis.FTC.effectiveFTC_riemannRaw_equiv_endpointDifference_of_endpointAgreement}
\lean{ComputableAnalysis.FTC.effectiveFTCStabilizedRaw_valid}
\lean{ComputableAnalysis.FTC.effectiveFTCStabilizedRaw_equiv_endpointDifference}
\lean{ComputableAnalysis.effectiveFTCStabilizedConstructionFor}
\lean{ComputableAnalysis.effectiveFTCStabilizedIntegralFor_valid}
\lean{ComputableAnalysis.effectiveFTCStabilizedIntegralFor_equiv_endpointDifference}
\leanok

The interface is the reusable calculus API: finite secant bounds feed
derivative certificates; derivative bounds feed partition boxes; the effective
FTC turns those boxes into endpoint differences. Monomial and finite
Taylor-prefix instances are checked independently of any completed
real-number topology.

The normalized monomial certificate can be scaled by its denominator for every
natural exponent.  This gives the finite power rule
$d(x^{n+1})/dx=(n+1)x^n$ on a bounded rational chart; the cubic below is its
first named nonlinear checkpoint.

\lean{ComputableAnalysis.FinitePolynomial.monomialSecantDerivativeBound}
\lean{ComputableAnalysis.FinitePolynomial.monomial_hasDerivativeOnInterval}
\lean{ComputableAnalysis.FinitePolynomial.normalizedMonomial_endpointDifference_contains_of_pos}
\leanok

As a concrete polynomial checkpoint, the normalized $x^3/3$ certificate is
rationally scaled to obtain $x^3$ with derivative $3x^2$.  The cubic
therefore enters the FTC ladder through the same finite secant machinery as
the general monomial family.

\lean{ComputableAnalysis.FinitePolynomial.cubeSecantDerivativeBound}
\lean{ComputableAnalysis.FinitePolynomial.cube_hasDerivativeOnInterval}
\leanok

The raw stabilized evaluator is now packaged at the domain-aware integral
layer.  An application supplies its primitive, integrand, finite endpoint
agreement, Riemann certificate, and shrinking radius; the resulting public
integral is a valid `RealRaw` and is equivalent to the primitive's endpoint
difference.

The proof order is intentionally finite:

\begin{verbatim}
input interval -> secant box -> derivative box -> mesh sum
             -> endpoint-difference box -> requested-width witness
\end{verbatim}

At no point is a derivative required to be an attained value at an irrational
point.  A derivative is a raw evaluator together with a modulus and a finite
containment theorem.  A classical pointwise statement is recovered only when
the boxes have been shown to shrink and the application needs that stronger
form.

When a selected stage schedule is not nested, prefix stabilization is the
generic repair: it intersects the finite candidate with a finite-radius
prefix envelope, using the endpoint difference only in the proof.  The
runtime remains a finite rational computation, while validity and equivalence
are supplied by width and radius certificates.

\section{Secants before completeness}
\label{thm:finite-quadrature-mean-value}

Historically, the mean-value relation became a way to expose the order
structure behind calculus. Here it is first stated as a finite bracket.
\begin{verbatim}
endpoint increment -> derivative bounds -> rational witness interval
\end{verbatim}
Monotone derivatives and convex functions give the cleanest first cases.
For a convex function, the secant over a cell lies between the neighboring
secants.  Thus a finite mesh supplies both the lower and upper derivative
boxes; refining the mesh is the effective replacement for invoking a least
upper bound.

The first formal mean-value statement is deliberately about the average, not
an existential point:

\begin{verbatim}
positive quadrature + lower <= f(x) <= upper
    -> lower <= (integral / (b-a)) <= upper
\end{verbatim}

This is the computable core of the classical claim that some derivative or
function value realizes an average.  If a later application can provide a
finite inverse or witness search, it may refine the bracket to an attained
point; the foundation does not assume such a point exists as an available
real-number object.

\lean{ComputableAnalysis.Integral.finiteQuadrature_average_between_bounds}
\leanok

\subsection{A cubic checkpoint}

The cubic makes the distinction visible. On $[0,1]$, the secant slope of
$x^3$ is $1$, while the normalized derivative target is $1/3$. The latter
need not have a rational square root, so the project records a bisection
interval for the intermediate point. On $[2,11]$, by contrast, the witness
$t=7$ is rational and the classical-looking identity is checked directly:

\begin{verbatim}
secant(x^3; 2,11) = 147 = 3 * 7^2
2 < 7 < 11
\end{verbatim}

Thus the same interface supports both outcomes: a finite witness when one is
available, and a shrinking rational search box when it is not.

\lean{ComputableAnalysis.cubicUnitNormalizedDerivativeAverage_in_unit}
\lean{ComputableAnalysis.cubicUnit_mvt_bisection_search}
\lean{ComputableAnalysis.cubic_mvt_unit_interval_certificate}
\lean{ComputableAnalysis.cubic_mvt_rational_witness_interval}
\leanok

\section{Taylor}
\label{thm:finite-taylor-ftc-prefix}

\begin{verbatim}
finite prefix -> finite identity -> remainder budget -> enclosure
\end{verbatim}
Taylor formulas and the arctangent kernel are instances of this FTC-style
reconstruction.
The reusable contract is a finite coefficient identity plus a remainder
budget.  It supports termwise integration whenever the transported tail is
within the requested rational width:

\begin{verbatim}
coefficients -> finite derivative identity -> tail enclosure
            -> integrated prefix -> endpoint comparison
\end{verbatim}

\section{Finite L'Hôpital}
\label{thm:finite-lhopital-interface}

Cancel a common factor, expose the finite remainder, and bound it rationally;
no attained limit is required.
The resulting theorem is a precision statement: after cancellation, the
quotient interval lies within the target interval at a computably chosen
stage.  The usual limit notation is an optional presentation of this family
of finite witnesses, not a primitive of the foundation.
