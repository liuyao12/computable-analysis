\chapter{Formalization Roadmap}

\section{Admission rule}

Keep a result when it yields a rational certificate, a shrinking `realRaw`
algorithm, a valid `real`, or an overlap statement.  Reuse routine finite
combinatorics unless it controls an infinite process or an error.

\section{Order}

\begin{verbatim}
intervals -> circle/rectangles -> tails/series/pi
          -> secants/MVT/FTC -> algebraic isolation -> linear ODEs
\end{verbatim}

\section{Wiedijk's List}
\label{sec:100-theorem-alignment}

The preface indexes the scoped entries in Freek Wiedijk's benchmark, linking
each to its rational, finite, or certificate-level blueprint statement.

\section{Style}

The blueprint gives only the idea, pseudocode, and GIF.  Lean carries names,
hypotheses, moduli, and proofs.  Only `realRaw` and `real` are named here.
