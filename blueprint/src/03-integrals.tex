\chapter{Integrals and the Fundamental Theorem}\label{ch:integrals}

The motivating integral is circle area: crude rectangles produce nested
rational bounds for the area under the arctangent kernel. An integral is a
program returning such bounds. Lebesgue measure is outside scope.

This chapter is where the Fundamental Theorem of Calculus belongs. It is the
first general bridge from local change to accumulated area; later chapters
only instantiate and reuse its certificate interface.  We use a two-stage
presentation: state the theorem here immediately after the first area
algorithm, then prove and package its reusable derivative-bound interface in
Chapter~\ref{ch:effective-calculus}.

The order is intentional:

\begin{verbatim}
circle/arctan example -> rectangle integral -> effective FTC
                      -> power series and exp/log instances -> ODEs
\end{verbatim}

FTC is introduced immediately after the first computable integral, then used
as the common interface for the remaining chapters.

The blueprint writes sine only as \(\sin(\pi x)\).  A normalized quarter-turn
parameter may occur inside the circle algorithm as an implementation detail.

\section{FTC as the organizing contract}
\label{sec:integrals-ftc-contract}

Once the rectangle algorithm has produced a certified area, the next question
is not another special function.  It is the general finite statement that
turns a derivative computation into an endpoint difference:

\begin{verbatim}
derivative bounds -> cell rectangles -> finite telescoping sum
                  -> endpoint difference within the same rational box
\end{verbatim}

This is the computable form of the FTC.  The theorem is introduced at this
point because it explains why integration is useful; its hypotheses are then
implemented by Lipschitz, monotone, convex, power-series, and turning-point
certificates.  Chapter~\ref{ch:effective-calculus} contains that reusable
implementation layer.  Chapters~\ref{ch:infinite-series}--\ref{ch:differential-equations}
consume the contract rather than reopening the foundations.

\section{Rectangle algorithm}

The question is not how to choose the best subdivision. Every certified
subdivision is a computation, and the theory relates all of them.

For each cell, certify monotonicity or isolate finitely many turns.  A turn is
represented by a nested rational bracket; the left, middle, and right boxes
are then assembled by finite interval addition.  The assembled width is
proved to shrink.  Only the final comparison with the intended integral is
function-specific, and is supplied as an explicit completion certificate.
\begin{verbatim}
mesh -> cell classification -> lower/upper rectangles -> overlap
\end{verbatim}
This gives the computable mean-value/area principle for the functions covered
by the certificates.
\begin{figure}[!htbp]\centering
\arctanRectangleEnclosureAnimation
\caption{Certified lower and upper rectangles.}
\label{fig:arctan-rectangle-enclosure}\end{figure}

For $\sin(\pi x)$ on $[0,1/2]$, the endpoints are exact and each interior
sample has a stage-dependent value bracket.
\begin{figure}[!htbp]\centering
\intervalSineIntegralStageAnimation
\caption{Value brackets and rectangle bounds.}
\label{fig:interval-sine-integral-stage}\end{figure}

\section{Monotone pieces and finitely many turns}

The usable generalization is piecewise, not an unrestricted measurable
function.  A monotone cell uses its endpoint range as the Darboux bracket;
finitely many turning points split a function into such cells.  The same
rectangle computation then adds the cell brackets.

\begin{verbatim}
finite breakpoints -> monotone cells -> endpoint ranges
                  -> add cell brackets -> one integral raw
\end{verbatim}

The Lean layer separates the geometric classification from the computation:
the monotone schedule supplies the dyadic ranges, while a piecewise-monotone
certificate supplies the finite assembly.  This is the route used for
functions such as \(\sin(\pi x)\): classify each cell as increasing,
decreasing, or containing one certified turn, then refine only the local
boxes needed by that classification.

\lean{ComputableAnalysis.MonotoneOnInterval}
\lean{ComputableAnalysis.Integral.MonotoneDarbouxSchedule}
\lean{ComputableAnalysis.Integral.monotoneDarbouxScheduleRaw_valid}
\lean{ComputableAnalysis.Integral.PiecewiseMonotoneConstructionFor}
\lean{ComputableAnalysis.Integral.piecewiseMonotoneIntegralFor_valid}
\lean{ComputableAnalysis.Integral.constructionFor_of_curvatureFTC}
\lean{ComputableAnalysis.Integral.definiteIdentityFor_of_curvatureFTC}
\leanok

The dyadic implementation uses the normalized angle \(2x=k/2^n\).  Its
finite stage is an explicit indexed rectangle sum:

\begin{verbatim}
for k in range(2^n):
    (sine, cosine) = nested_radical_table(n, k)
    add (1/2^(n+1)) * sine to the interval sum
\end{verbatim}

The finite sum is packaged as a raw candidate. The remaining certificate
proves that the half-angle boxes enclose the intended values and that the
resulting sums are nested and shrink. Only after that certificate is supplied
does the raw candidate become a computable integral.

The implementation keeps mesh level and evaluation precision separate:
each stage rebuilds its nested-radical paths at the current precision.  This
prevents an old, coarse parent box from being reused forever at refined
stages.

Each square-root box is then clipped to its geometric range \([0,1]\).  The
exact root remains enclosed, while the clipping makes the range invariant
inductive: lower-half cosine boxes lie in \([0,1]\), reflected boxes in
\([-1,0]\), and sine boxes in \([0,1]\). Lean proves this finite geometric
invariant directly.

\section{The first general integrability theorem}

The project does not claim that every continuous function is integrable by
default. It proves the stronger-for-computation statement for an explicit
rational Lipschitz certificate:

\begin{verbatim}
f : rational evaluator on [0,1]
L : certified Lipschitz bound
        |
        v
dyadic cells -> lower/upper Lipschitz rectangles
                 -> nested raw integral
                 -> width <= 2*L/2^stage
\end{verbatim}

This is the foundation's replacement for a completeness-based existence
theorem. The rate is visible, executable, and can be transported to a
special-function evaluator. A merely effective modulus of continuity can be
added later by supplying a corresponding cell-width schedule; it is not
silently identified with the Lipschitz case.

\lean{ComputableAnalysis.Integral.LipschitzOnUnit}
\lean{ComputableAnalysis.LipschitzDyadic.compute}
\lean{ComputableAnalysis.LipschitzDyadic.compute_width}
\lean{ComputableAnalysis.LipschitzDyadic.compute_nested}
\lean{ComputableAnalysis.LipschitzDyadic.raw_valid}
\lean{ComputableAnalysis.LipschitzDyadic.construction}
\leanok

The arctangent kernel is the first named instance: its bound $L=2$ gives
an interval width $4/2^n$ before any endpoint or FTC identification.

\lean{ComputableAnalysis.IntegralIdentities.oneOverOnePlusSquare_lipschitz_on_unit}
\lean{ComputableAnalysis.IntegralIdentities.arctanKernelLipschitzIntegral_valid}
\leanok

The finite base cases are explicit as well.  Constants and affine functions
are integrated by rational endpoint formulas; their monotonicity certificates
are retained so the same constructions can be inserted into a piecewise plan.

\lean{ComputableAnalysis.exactRat_constant_nondecreasing}
\lean{ComputableAnalysis.constantMonotoneIntegralFor_eq_ofRat}
\lean{ComputableAnalysis.exactRat_affine_nondecreasing}
\lean{ComputableAnalysis.affineMonotoneIntegralFor_eq_ofRat}
\lean{ComputableAnalysis.affineMonotoneIntegralFor_of_nonpos_eq_ofRat}
\leanok

\section{Beyond Lipschitz: effective monotone schedules}

The next layer accepts an interval-level modulus rather than one global
Lipschitz constant. For a nondecreasing evaluator, endpoint boxes on each
cell give a Darboux range. The finite width estimate is already general:

\begin{verbatim}
interval regularity + mesh/input budget
    -> endpoint Darboux boxes
    -> width <= (b-a)/(requested evaluation stage + 1)
\end{verbatim}

The public schedule records exactly what a proof must still establish:

\begin{verbatim}
pieces(n), precision(n)
    -> mesh budget
    -> nesting across stages
    -> widths shrink to zero
    -> valid computable integral
\end{verbatim}

\lean{ComputableAnalysis.IntervalRegularOn}
\lean{ComputableAnalysis.Integral.nondecreasingDarbouxDyadicStage_width_le_of_input_budget}
\lean{ComputableAnalysis.Integral.MonotoneDarbouxSchedule}
\lean{ComputableAnalysis.Integral.monotoneDarbouxScheduleRaw_valid}
\lean{ComputableAnalysis.Integral.monotoneDarbouxScheduleConstructionFor}
\lean{ComputableAnalysis.Integral.monotoneDarbouxScheduleIntegralFor_valid}
\leanok

\section{Functions that turn finitely many times}

The next usable class is not “all continuous functions.” It is a function
whose interval is split into finitely many monotone pieces, with each
non-rational turning point represented by a shrinking rational bracket. At
stage \(n\), integrate the certified left and right monotone pieces and put a
range enclosure over the unresolved middle bracket:

\begin{verbatim}
turn bracket -> left monotone box + middle range box + right monotone box
             -> finite rational sum -> shrinking integral candidate
\end{verbatim}

The middle contribution needs only a bound on the function range. Its width
is therefore bounded by the range bound times the turn-bracket width; no value at
the turn is required. This is the constructive form of the practical rule
used for \(\sin(\pi x)/x\): classify each cell as increasing, decreasing, or
containing one of finitely many certified turns, and refine only the latter.

\lean{ComputableAnalysis.Integral.TurningPointBracket}
\lean{ComputableAnalysis.Integral.TurningPointBracket.widths_shrink}
\lean{ComputableAnalysis.Integral.TurningBracketIntegralCandidate}
\lean{ComputableAnalysis.Integral.SingleTurnIntegralCandidate.compute_widths_shrink}
\lean{ComputableAnalysis.Integral.SingleTurnIntegralCompletion.stabilizedRaw_valid}
\lean{ComputableAnalysis.Integral.FinitePiecewiseStageAssembly.raw_widths_shrink}
\lean{ComputableAnalysis.Integral.MultiTurnIntegralCompletion.stabilizedRaw_valid}
\lean{ComputableAnalysis.Integral.SingleTurnIntegralCompletion.toMultiTurn}
\leanok

The last declaration is deliberately a completion certificate, not a
universal theorem. It says that a particular three-part candidate overlaps
its chosen endpoint representative. A future multi-turn constructor will
repeat this finite pattern and sum the pieces; it will not introduce Lebesgue
measure or a completed-real compactness argument.

This schedule layer is the honest frontier between the present foundation and
the classical theorem that every continuous function on a closed interval is
integrable. The missing work is constructive schedule synthesis and its finite
nesting proof; no completeness axiom is being hidden in that gap.

\section{First complete finite-turn example}

The absolute value is the smallest non-monotone test case. On $[-1,0]$ it is
the affine function $-x$; on $[0,1]$ it is the affine function $x$. The two
exact rational cell integrals are therefore each $1/2$, and the piecewise
assembly proves

\[
  \int_{-1}^{1} |x|\,dx = 1.
\]

This is a complete proof through the public general-integral interface: one
rational evaluator, one rational turn, two monotonicity certificates, and a
finite sum identified with a rational anchor. Later examples with irrational
turning points use the same interface; only the turn bracket and middle range
box become nontrivial.

\lean{ComputableAnalysis.Integral.absOnUnit}
\lean{ComputableAnalysis.Integral.absOnUnit_left}
\lean{ComputableAnalysis.Integral.absOnUnit_right}
\lean{ComputableAnalysis.Integral.absOnUnit_piecewise}
\lean{ComputableAnalysis.Integral.absOnUnit_piecewise_integral_valid}
\lean{ComputableAnalysis.Integral.absOnUnit_piecewise_integral_equiv_one}
\leanok

The geometric certificate is deliberately elementary: a parent point on the
unit circle with nonnegative coordinates has child coordinates satisfying

\begin{verbatim}
child_sine^2 = (1 - parent_cosine)/2
child_cosine^2 = (1 + parent_cosine)/2
child_sine, child_cosine >= 0
\end{verbatim}

Lean records this as a finite half-angle certificate. It is the formal
version of the geometric picture behind the nested radicals. The repository
currently keeps this layer self-contained rather than importing Mathlib's
real-number geometry; a future import would be useful only if it supplied the
same rational/interval statements without pulling in completed reals.

\section{The central bridge: an effective FTC}
\label{thm:convex-ftc}
\label{thm:effective-derivative-bound-ftc}

The historical core of calculus is the converse relation between local change
and accumulated area. Here the FTC is the chapter's central theorem: it is
proved from finite comparisons, not imported from completeness.

\begin{verbatim}
derivative bounds -> lower/upper rectangles -> endpoint difference -> overlap
\end{verbatim}

On each cell, a certified derivative bound controls the difference between
the rectangle sum and the primitive's endpoint increment.  For monotone or
convex/concave derivatives, neighboring secants provide the needed bounds;
finite turning-point certificates extend the construction piecewise.  The
result is an effective FTC: every requested output precision comes with a
finite partition and a rational error certificate.

The public contract is general:

\begin{verbatim}
candidate derivative -> cell bounds -> finite sum -> endpoint overlap
                                         -> shrinking raw integral
\end{verbatim}

The certificate does not prescribe how derivative bounds are obtained.
Monotonicity, convexity, power-series tails, and formula-specific interval
arithmetic all implement the same interface. Thus the theorem is reusable
without claiming that every function is integrable.

The formal spine is now exposed as four layers:

\begin{verbatim}
EffectiveFTC                 -> scheduled Riemann sums vs endpoint box
EffectiveDerivativeBoundFTC  -> local derivative bounds -> EffectiveFTC
Convex/concave FTC            -> secant bounds -> derivative-bound certificate
StaticDyadicEffectiveFTC      -> equal-dyadic specialization
\end{verbatim}

\lean{ComputableAnalysis.EffectiveFTC}
\lean{ComputableAnalysis.EffectiveDerivativeBoundFTC}
\lean{ComputableAnalysis.effectiveDerivativeBoundFTC}
\lean{ComputableAnalysis.FTC.effectiveFTC_definiteIntegralEqualsEndpoint}
\lean{ComputableAnalysis.FTC.staticDyadicEffectiveFTC_definiteIntegralEqualsEndpoint}
\leanok

The first non-polynomial instance is already closed: Lean constructs
`arctanEffectiveFTCData` for the rational kernel \(1/(1+x^2)\) and proves its
finite rectangle integral equivalent to the endpoint primitive. This is the
project's working FTC, not an appeal to a general real-number completeness
theorem.  The Lipschitz--Darboux version now closes the entire value chain:
\[
\text{rational rectangles}
\;\longrightarrow\;
\arctan(1)
\;\longrightarrow\;
\frac{\pi}{4}.
\]

\lean{ComputableAnalysis.IntegralIdentities.arctanKernelLipschitzIntegral}
\lean{ComputableAnalysis.IntegralIdentities.arctanKernelLipschitzIntegral_valid}
\lean{ComputableAnalysis.IntegralIdentities.arctanKernelLipschitzIntegral_equiv_rectangleForAtOne}
\lean{ComputableAnalysis.IntegralIdentities.arctanKernelLipschitzIntegral_equiv_arctanGeom_one}
\lean{ComputableAnalysis.IntegralIdentities.arctanKernelLipschitzIntegral_equiv_halfQuarterTurn}
\lean{ComputableAnalysis.ArctanGeometry.arctanGeom_one_valid}
\leanok

\lean{ComputableAnalysis.Integral.arctanEffectiveFTCData}
\lean{ComputableAnalysis.Integral.arctanEffectiveFTC_equiv_endpoint}
\lean{ComputableAnalysis.Integral.arctanEffectiveFTCStabilized_valid}
\lean{ComputableAnalysis.Integral.arctanEffectiveFTCStabilizedIntegral_equiv_endpointDifference}
\leanok

Chapter~\ref{ch:effective-calculus} packages this theorem for reuse: it does
not introduce a competing notion of integral or a second foundation.

The classical mean value theorem is the zero-width corollary.  At finite
precision, \texttt{endpoint\_difference\_overlaps\_bound} records its useful content:
the scaled derivative range overlaps the endpoint-increment box.  A literal
intermediate point is not selected; the bracket is refined until the desired
precision is reached.  We therefore recover MVT only where the function has
the required effective monotonicity or turning-point certificate.

The local-to-global proof is a finite telescoping argument:

\begin{verbatim}
cell derivative range -> cell width * range contains endpoint increment
all cells              -> finite sum contains F(b)-F(a)
width budget           -> effective FTC bracket
\end{verbatim}

These are separate reusable contracts, so a new function need only supply its
cell certificate and error budget:

\lean{ComputableAnalysis.CandidateDerivativeCellControl.endpoint_difference_overlaps_bound}
\lean{ComputableAnalysis.RationalPartition.boundIntegralSum_contains_endpointDifference}
\lean{ComputableAnalysis.RationalPartition.boundIntegralSum_overlaps_endpointDifference}
\lean{ComputableAnalysis.DerivativeBoundFTC}
\lean{ComputableAnalysis.effectiveFTC_equiv_endpoint}
\lean{ComputableAnalysis.effectiveFTC_definiteIntegralEqualsEndpoint}
\leanok

The checked polynomial ladder now reaches constant, affine, square, cube,
quartic, and quintic evaluators, with a finite sextic Darboux checkpoint; the
square case uses the finite bound
\(v^2-u^2=(v-u)(v+u)\le 2(v-u)\) on \([0,1]\); the cube case uses the
analogous three-term factorization and modulus \(3(n+1)\).  The corresponding
finite dyadic integrals are identified with \(1/3,1/4,1/5,1/6\) by overlap
with rational singleton intervals. The reusable polynomial layer also gives
finite secant certificates for every normalized monomial and every finite
Taylor prefix; these are the main inputs for non-polynomial functions.  Thus
the foundation separates the uniform theorem from its finite value algebra:
the derivative/FTC certificate is degree-independent, while each claimed
closed value is backed by its own rational power-sum identity.

\lean{ComputableAnalysis.FinitePolynomial.normalizedMonomialSecantBound}
\lean{ComputableAnalysis.FinitePolynomial.normalizedMonomial_hasDerivativeOnInterval}
\lean{ComputableAnalysis.FinitePolynomial.taylorPrefix_hasDerivativeOnInterval}
\leanok

For the cubic case the first rectangle identity is explicit:
\(\sum_{k<n}3(k/n)^2(1/n)\) is evaluated by a finite sum-of-squares
induction.  The interval-regular layer then supplies the domain-restricted
effective FTC certificate used for the complete \(x^3\) instance.

\lean{ComputableAnalysis.riemannLeftExact_threeSquare_unit_of_pos}
\lean{ComputableAnalysis.Integral.cubicIntegralEffectiveFTC_equiv_one_fourth}
\leanok

The first non-polynomial test is a squared sine prefix.  Its finite Taylor
surrogate is integrated by the same effective FTC and evaluated by a rational
certificate (the current prefix value is \(6389/161280\)).  The exact
tangent-chart route is also exposed: its rational derivative certificate
transports the integral to the quarter-turn anchor \(\pi/4\).  The endpoint
schedule is made nested by finite prefix stabilization and is now a verified
raw real.  The integral-side certificate has two parts: validity of the finite
Riemann-sum raw computation, and a same-stage overlap bridge from that
computation to the canonical endpoint box.  The tangent-chart proof now
constructs the public integral raw by anchor-reboxing: finite boxes are
intersected with the nested endpoint anchor, so validity follows from ordered
widths, an explicit \(2/(n+1)\) shrink bound, and the overlap theorem.  Lean
also proves constructively that the chosen endpoint stage is no earlier than
the public stage: the positive rational stage budget has a denominator large
enough to dominate the requested stage.  The equal-dyadic \(\sin^2\) route
separately needs its arctangent-backed regularity and endpoint certificate
before it is identified with \(1/4\).  For the unsquared sine route, the
remaining certificate is target-directed: each nested-radical sample must be
matched to a tangent box by a finite bisection trace or half-angle witness.

The tangent-chart value theorem is already public:

\begin{verbatim}
finite tangent-chart FTC -> tangentChartIntegral = 1/pi
                         -> public sine half-integral = 1/pi
\end{verbatim}

The equal-dyadic route is not silently identified with that chart.  Its exact
remaining obligation is the finite `TangentChartTransport` structure, whose
fields say that the public dyadic box and the chart box overlap at every
stage.  A common rational witness is an equivalent, more constructive form.

The squared-sine route is a useful intermediate application: squaring the
same certified samples gives a monotone Darboux integral, and the FTC/value
transport identifies it with \(1/4\) once the endpoint certificate is supplied.

The reusable interval-transfer step is now generic: after the correctly
scaled raw integral is identified with its target, combine its stage width
with the finite rectangle margin.  The scaling must come first: the unscaled
tangent-chart integral represents the quarter-turn, while the normalized
squared-sine integral represents \(1/4\).

\lean{ComputableAnalysis.finite_sum_close_of_raw_equiv_of_margin}
\leanok

\lean{ComputableAnalysis.SinPiIntegral.sinPiSquareMonotoneIntegral}
\lean{ComputableAnalysis.SinPiIntegral.sinPiSquareMonotoneIntegral_valid}
\lean{ComputableAnalysis.SinPiIntegral.sinPiSquareMonotoneIntegral_equiv_value}
\lean{ComputableAnalysis.SinPiIntegral.tangentSquareEffectiveFTCData}
\lean{ComputableAnalysis.SinPiIntegral.tangentSquareEffectiveFTC_integral_equiv_halfQuarterTurn}
\lean{ComputableAnalysis.reciprocalPi_quarterTurn_equiv_quarter}
\lean{ComputableAnalysis.NormalizedTangentSquareValueSubgoal.value}
\lean{ComputableAnalysis.TangentSquareLeftSumQuarterCertificate}
\lean{ComputableAnalysis.TangentSquareLeftSumQuarterCertificate.to_value_subgoal}
\lean{ComputableAnalysis.tangentSquareLeftSum_stage_one_quarter_certificate}
\lean{ComputableAnalysis.tangentSquareLeftSum_stage_two_quarter_certificate}
\lean{ComputableAnalysis.tangentSquareLeftSum_stage_three_quarter_certificate}
\lean{ComputableAnalysis.tangentSquareLeftSum_stage_four_quarter_certificate}
\lean{ComputableAnalysis.TangentSquareFTCIntegralCommonWitness}
\lean{ComputableAnalysis.TangentSquareFTCIntegralCommonWitness.to_compatibility}
\leanok

\lean{ComputableAnalysis.SinPiIntegral.computableSinPiHalfIntegral_equiv_reciprocalPi_of_FTC}
\lean{ComputableAnalysis.SinPiIntegral.ArctanSinPiConstruction.TangentChartTransport}
\lean{ComputableAnalysis.SinPiIntegral.ArctanSinPiConstruction.halfIntegral_equiv_reciprocalPi_of_transport}
\lean{ComputableAnalysis.SinPiIntegral.ArctanSinPiConstruction.tangentChartTransport_of_staticFTC_of_endpoint_equiv}
\lean{ComputableAnalysis.SinPiTransportSubgoals.DyadicNestedRadicalBranchCertificateFamily.toWitnessFamily}
\leanok

The preferred finite bridge is a rational witness search, rather than equality
of the two interval implementations.  A rational unit-circle point is placed
inside the nested-radical sine and cosine boxes, and its half-angle tangent
box is placed inside the inverse-tangent box.  The circle identity then proves
the required sine-box overlap:

\lean{ComputableAnalysis.SinPiIntegral.CanonicalDyadicHalfAngleCertificateAt}
\lean{ComputableAnalysis.SinPiIntegral.canonical_dyadic_overlap_of_halfAngle_outer_tangent_at}
\lean{ComputableAnalysis.SinPiIntegral.canonical_dyadic_certificate_at_of_rational_witness}
\lean{ComputableAnalysis.SinPiIntegral.DyadicTangentWitnessFamily.of_canonical_search_family}
\leanok
The integral and Darboux layers do not require a completed real sine function.

The closed finite instances are linked directly to their certificates:

\lean{ComputableAnalysis.FiniteSinePrefix.sineTaylorPrefixOne_eq_identity}
\lean{ComputableAnalysis.FiniteSinePrefix.sineTaylorPrefixOneIntegralRaw_equiv_one_eighth}
\lean{ComputableAnalysis.FiniteSinePrefix.sineTaylorPrefixThreeEffectiveFTCData}
\lean{ComputableAnalysis.FiniteSinePrefix.sineTaylorPrefixThreeEffectiveFTC_equiv_endpoint}
\lean{ComputableAnalysis.FiniteSinePrefix.sineTaylorPrefixThreeSquareEffectiveFTCData}
\lean{ComputableAnalysis.FiniteSinePrefix.sineTaylorPrefixThreeSquareEffectiveFTC_equiv_endpoint}
\leanok

The target-directed inverse search has the same shape as the integral
algorithm.  A cell is bisected, its certified forward image is compared with
the target box, and one Boolean decision is retained:

\begin{verbatim}
cell := [lo,hi]
mid  := (lo+hi)/2
if forward_image([lo,mid]) is below target:
    cell := [mid,hi]
else:
    cell := [lo,mid]
record target-overlaps-forward-image(cell)
\end{verbatim}

The trace itself is a rational computation.  Its proof fields are source
containment, halving width, and the final overlap at every requested stage.
This is the finite inverse certificate consumed by the nested-radical sine
integral.

The even nested-radical branch is now packaged directly from a successful
finite tangent-witness search.  It reuses the public circle box, proves its
ordering, and transports the search overlap to the parent table precision:

\lean{ComputableAnalysis.SinPiIntegral.DyadicEvenStepCertificate.ofWitnessSearch}
\lean{ComputableAnalysis.SinPiIntegral.DyadicEvenStepCertificate.to_public_overlap}
\lean{ComputableAnalysis.SinPiTransportSubgoals.DyadicEvenStepCertificate.of_overlap}
\leanok

There is also a shorter completion route.  Instead of requiring exact
half-angle interval identities, supply a finite rational tangent-witness
search for every sampled box.  The public integral needs only the resulting
stagewise overlap, and Lean already assembles those overlaps into the
equal-dyadic integral:

\begin{verbatim}
sample box + finite witness search -> sine/table overlap
all dyadic samples             -> integral equivalence
\end{verbatim}

\lean{ComputableAnalysis.SinPiIntegral.DyadicTangentWitnessFamily}
\lean{ComputableAnalysis.SinPiIntegral.ArctanSinPiConstruction.halfIntegral_equiv_of_canonical_nestedRadical_search}
\lean{ComputableAnalysis.SinPiIntegral.ArctanSinPiConstruction.halfIntegral_equiv_of_witness_family}
\leanok

The exact even/lower/reflected certificate family remains the geometric
explanation; the witness-search adapter is the minimal proof obligation for
closing the numerical integral.  The weaker but directly usable route is now
explicit: canonical rational unit-circle certificates discharge all three
nonzero parity branches without asserting equality between the square-root
and circle interval evaluators; the zero endpoint remains exact.

The unrestricted unit tangent chart is already complete: a finite dyadic grid
hits every positive-width sine target, and the Boolean witness search returns
that hit.  The remaining difficulty is only restricting the search to the
smaller inverse-arctangent box at each dyadic sample.

\lean{ComputableAnalysis.SinPiIntegral.rationalCircleSin_dyadic_grid_hit}
\lean{ComputableAnalysis.SinPiIntegral.rationalTangentWitnessSearch_unit_complete}
\leanok

Two concrete searches are already checked as finite sanity tests.  They use
the grid candidates (103/256) and (791/4096) at successive dyadic stages.
The reusable soundness theorem converts any successful search into the
circle-box overlap required by the transport step:

\lean{ComputableAnalysis.SinPiIntegral.rationalTangentWitnessSearch_stage_one_demo}
\lean{ComputableAnalysis.SinPiIntegral.rationalTangentWitnessSearch_stage_two_demo}
\lean{ComputableAnalysis.SinPiIntegral.rationalTangentWitnessSearch_overlap_of_success}
\lean{ComputableAnalysis.SinPiIntegral.rationalTangentWitnessBoxSearch_overlap_of_success}
\leanok

When interval arithmetic proves only overlap, the exact-box equality can be
avoided altogether.  The direct branch-overlap adapter accepts the three
finite even/lower/reflected overlap families and assembles them into the same
search witness family:

\begin{verbatim}
even overlap + lower overlap + reflected overlap
    -> all dyadic sample overlaps -> integral transport
\end{verbatim}

\lean{ComputableAnalysis.SinPiIntegral.DyadicTangentWitnessFamily.of_branch_overlap_families}
\lean{ComputableAnalysis.SinPiTransportSubgoals.lower_overlap_of_canonical_halfAngle_certificate}
\lean{ComputableAnalysis.SinPiTransportSubgoals.upper_overlap_of_canonical_halfAngle_certificate}
\lean{ComputableAnalysis.SinPiTransportSubgoals.DyadicTangentWitnessFamily.of_canonical_halfAngle_families}
\lean{ComputableAnalysis.SinPiTransportSubgoals.ArctanSinPiConstruction.halfIntegral_equiv_of_canonical_halfAngle_families}
\lean{ComputableAnalysis.SinPiTransportSubgoals.ArctanSinPiConstruction.halfIntegral_equiv_reciprocalPi_of_canonical_halfAngle_families}
\leanok

The lower and reflected constructors make the remaining proof obligations
minimal.  Their parent boxes are taken from the nested table, so only the
finite identity between the public circle box and the clipped half-angle
square-root box remains to be supplied:

\begin{verbatim}
table cosine box -> clipped half-angle sqrt box
                 = public circle-sine box
                 -> branch certificate
\end{verbatim}

\lean{ComputableAnalysis.SinPiIntegral.DyadicHalfAngleChildCertificate.of_table_parent}
\lean{ComputableAnalysis.SinPiIntegral.DyadicReflectedHalfAngleCertificate.of_table_parent}
\leanok

The search itself is proof-producing: a successful Boolean hit is converted
into the complete half-angle certificate consumed by the transport theorem.

The final adapter makes the proof boundary explicit.  One supplies a finite
candidate list for each dyadic sample and proves that the executable search
has a hit.  The adapter converts those finite successes into the witness
family used by the integral theorem; no completeness or unbounded search is
hidden in the interface.

\lean{ComputableAnalysis.SinPiIntegral.canonicalDyadicCertificateSearchAt}
\lean{ComputableAnalysis.SinPiIntegral.canonicalDyadicCertificateSearchAt_sound}
\lean{ComputableAnalysis.SinPiIntegral.DyadicTangentWitnessFamily.of_canonical_search_family}
\lean{ComputableAnalysis.SinPiIntegral.DyadicTangentWitnessFamily.of_branch_certificate_families}
\lean{ComputableAnalysis.SinPiIntegral.ArctanSinPiConstruction.halfIntegral_equiv_of_branch_certificates}
\lean{ComputableAnalysis.SinPiIntegral.ArctanSinPiConstruction.halfIntegral_equiv_reciprocalPi_of_branch_certificates}
\leanok

\begin{verbatim}
sine prefix   -> square prefix -> derivative bounds -> FTC endpoint box
tangent chart -> rational derivative certificate -> endpoint = quarter-turn
equal dyadic  -> regularity + endpoint identity -> integral = 1/4
\end{verbatim}

\section{Riemann--Stieltjes viewpoint}

Uneven samples are finite Riemann--Stieltjes sums: a monotone chart supplies
the increments of the integrator, while the function supplies the sampled
values.  For the sine example, use
\(x(u)=2\arctan(u)/\pi\).  Equal subdivisions in \(u\) therefore produce
unequal subdivisions in \(x\), computing
\(\int_0^{1/2}\sin(\pi x)\,dx\) through Stieltjes sums.

\begin{verbatim}
uniform u-mesh -> inverse-chart x-samples -> Stieltjes rectangles -> overlap
                         |
                         +-- substitution theorem (when needed)
\end{verbatim}

This construction is checked in Lean; its earlier name remains only as a
compatibility alias.

\lean{ComputableAnalysis.SinPiIntegral.sinPiStieltjesIntegral}
\lean{ComputableAnalysis.SinPiIntegral.sinPiStieltjesIntegral_valid}
\lean{ComputableAnalysis.SinPiIntegral.sinPiStieltjesIntegral_equiv_reciprocalPi}
\lean{ComputableAnalysis.SinPiIntegral.computableSinPiHalfIntegral}
\lean{ComputableAnalysis.SinPiIntegral.computableSinPiHalfIntegral_valid}
\lean{ComputableAnalysis.SinPiIntegral.computableSinPiHalfIntegral_equiv_reciprocalPi}
\lean{ComputableAnalysis.SinPiIntegral.computableSinPiHalfIntegral_equiv_reciprocalPi_of_FTC}
\lean{ComputableAnalysis.SinPiIntegral.sinPiUnevenSamplingIntegral}
\leanok

This is the first completed end-to-end sine integral in the project.  It uses
uneven samples in the original (x)-coordinate, but uniform subdivisions in
the rational tangent coordinate (u); the equal-dyadic version remains a
separate, stronger transport theorem.

\section{Finite identities}
\label{thm:finite-green-rectangle}
\label{def:integration-by-parts-certificate}
\label{thm:integration-by-parts-certificate}
\label{cor:arctangent-logarithm-pi-route}

\begin{verbatim}
finite boundary sum -> algebraic identity -> endpoint difference
                                      -> certified error budget
\end{verbatim}
Green's rectangle identity, Stieltjes transport, substitution, telescoping, integration by parts,
and polynomial FTC are instances of this finite pattern.

The algebraic order is deliberate: first prove identities for one finite
partition, then attach a shrinking certificate.  The Lean interface exposes
the main transport rules:

\lean{ComputableAnalysis.Integral.integral_equiv_of_plan_and_samples}
\lean{ComputableAnalysis.Integral.integral_equiv_of_plan_and_sample_overlaps}
\lean{ComputableAnalysis.finiteIntegrationByParts_onPartition}
\lean{ComputableAnalysis.finiteIntegrationByParts_withVariation_onPartition}
\lean{ComputableAnalysis.coordinateIntegrationByParts_onPartition_endpoint_bracket}
\lean{ComputableAnalysis.RationalPartition.endpointDifferenceSum_contains}
\lean{ComputableAnalysis.RationalPartition.boundIntegralSum_overlaps_endpointDifference}
\leanok

Linearity and additivity are stated as certificate contracts:

\begin{verbatim}
same finite plan + sample transport -> equivalent integrals
adjacent endpoint boxes -> telescoping endpoint box
finite partition identity + shrinking widths -> computable integral law
\end{verbatim}

\lean{ComputableAnalysis.Integral.Linear}
\lean{ComputableAnalysis.Integral.CompatibleWithScaleRat}
\lean{ComputableAnalysis.Integral.AdditiveOnAdjacentIntervals}
\leanok

The first algebra law is already proved at the raw level.  For rational
endpoints \(a\le b\le c\), the two finite endpoint boxes telescope:

\begin{verbatim}
[F(b)-F(a)] + [F(c)-F(b)]  ->  [F(c)-F(a)]
        finite interval arithmetic, with overlap at every stage
\end{verbatim}

No limit law is being assumed here.  The definite-integral theorem then
transports this finite identity through three FTC certificates.  Thus the
usual additivity statement is available exactly when the three integrals have
certified endpoint representations.

\lean{ComputableAnalysis.endpointDifferenceRaw_adjacent_additive}
\lean{ComputableAnalysis.FunctionOnInterval.endpointDifferenceRaw_adjacent_additive}
\lean{ComputableAnalysis.DefiniteIdentityFor.integral_add_equiv_of_endpoint_additive}
\lean{ComputableAnalysis.DefiniteIdentityFor.integral_equiv_add_of_endpoint_add}
\leanok

This is the constructive replacement for silently importing a global vector
space of integrable functions.  Each concrete family supplies the construction
and the finite certificates required by the contract.

The sine integral in Chapter~\ref{ch:rational-circle-trigonometry} is the
first integration target: the circle/arctangent construction supplies the
values, the rectangle algorithm supplies the area, and this FTC supplies the
endpoint identity once its finite trigonometric certificate is instantiated.

\section{Tails and products}

Compactify the domain, bound the tail, and combine finite bounds. Multiple
integrals are iterated finite enclosures; Gaussian and $n$-ball formulas are
the main examples.
\begin{verbatim}
finite boxes -> product bounds -> tail certificate -> nested enclosure
\end{verbatim}

\section{Interface}

\begin{verbatim}
intervals -> monotone pieces -> finite identities -> overlap
\end{verbatim}
